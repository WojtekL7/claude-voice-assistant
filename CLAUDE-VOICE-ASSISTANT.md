# CLAUDE-VOICE-ASSISTANT — Agent aplikacji desktopowej

**Przed pracą załaduj również:** 🔴 [`docs/PRD.md`](docs/PRD.md) (roadmap komercjalizacji 2026) · [`../CLAUDE-COMMON.md`](../CLAUDE-COMMON.md) (procedury, pułapki uniwersalne) · [`CLAUDE.md`](CLAUDE.md) (auto-ładowany w tym katalogu).

> **Historia, pełne narracje i wycięte szczegóły:** `CLAUDE-VOICE-ASSISTANT-ARCHIVE.md` (NIE czytany na starcie) + `git log`.
> **Pliki TEMATYCZNE — ⛔ czytaj TYLKO gdy ruszasz ten temat, nigdy na starcie:**
> · `CLAUDE-VOICE-ASSISTANT-PULAPKI.md` — trwałe pułapki projektu (Qt/QSS, pasek przycisków, WebTerminal,
>   PyInstaller/pakowanie, wykrywanie `claude`), architektura terminala i auto-czytania, sygnały AgentTab,
>   diagnoza crashu `claude` + „czarna skrzynka".
> · `CLAUDE-VOICE-ASSISTANT-WYDANIA.md` — runbook wydania, self-update Linuksa (AppImage), strony instrukcji.
> **Utrzymanie:** tylko trwałe, aktualne, reużywalne rzeczy. Bez sekcji „SESJA <data>", bez stopek-dzienników. Budżet ~350 linii — przy przekroczeniu konsoliduj (przepis: COMMON „ODCHUDZANIE PLIKÓW PAMIĘCI"; sukces mierz ZNAKAMI, nie liniami).

---

## Projekt

- **Lokalizacja:** `/home/hdkrytbhdkf/Projekty/claude-voice-assistant/` · **GitHub:** https://github.com/WojtekL7/claude-voice-assistant
- **Tech:** Python 3.12, PyQt5, QTermWidget (Linux), WebTerminal = xterm.js+QtWebEngine+PTY (Mac/Win), edge-tts, Groq Whisper, pygame
- **Czym jest:** „pilot" nad CLI **Claude Code** — uruchamia `claude` w terminalu, dokłada głos (TTS/STT), zakładki agentów, MCP/Skills, skórki, auto-aktualizację. **Apka NIE hostuje modelu ani nie zawiera CLI** — konto/logowanie siedzi u usera (`~/.claude`).

```bash
cd /home/hdkrytbhdkf/Projekty/claude-voice-assistant && source venv/bin/activate && python3 src/main.py
```
WebTerminal na Linuksie do testów: `CVA_WEBTERMINAL=1 python3 src/main.py`. Wheel: `wheels/qtermwidget-1.4.0-cp310-abi3-manylinux_2_17_x86_64.whl`.

| Plik | Rola |
|------|------|
| `src/config.py` | Języki, głosy TTS, modele, ścieżki, `APP_VERSION`, `UI_TRANSLATIONS`, `DEFAULT_SPLITTER_SIZES` |
| `src/gui/main_window.py` | Główne okno, menu, TTS/STT, skórki, zakładki, auto-update, `_poll_transcripts` |
| `src/gui/agent_tab.py` | Zakładka agenta: terminal, splitter, input, przyciski |
| `src/gui/dialogs.py` | Dialogi (4-tab konfiguracja agenta, update, ClaudeSetupDialog) |
| `src/gui/terminal_backend.py` | Wspólny interfejs terminala + fabryka (QTermWidget vs WebTerminal) |
| `src/gui/web_terminal.py` | WebTerminal (xterm.js+QtWebEngine+PTY); ConPTY na Windows |
| `src/gui/theme.py` | Paleta „Vibe Purple" — JEDYNE źródło kolorów |
| `src/core/tts_engine.py` | TTS (edge-tts + pygame), kolejka z prefetch |
| `src/core/transcript_reader.py` | Czyta dziennik sesji `.jsonl` (auto-czytanie, flaga „?", `turn_snapshot()`) |
| `src/core/text_cleaner.py` | `prose_from_markdown()` — proza dla TTS |
| `src/core/update_manager.py` | Self-update (pobieranie, sha256, samo-podmiana per OS) |
| `src/core/platform_utils.py` | OS/arch, env Qt, `update_platform_id()`, `macos_app_bundle()`, `claude_runnable()` |
| `tools/scan-dialog-clipping.py` | Skan regresji: 16 okien offscreen, zgłasza widżety ucinające tekst |
| `tools/test-terminal-grid.py` | Bramka siatki terminala (kratka na znak) — odtwarza „rozstrzelone litery" sabotażem |
| `tools/test-bottom-bar-icons.py` | Bramka dolnego paska: kolejność przycisków + styl lupy, mierzone na pikselach |
| `tools/test-cleanup.py` | Bramka sprzątania przy starcie: co ginie, a co MUSI przeżyć (atrapa HOME) |

⚠️ **Zbiorczy przebieg bramek: NIE licz wyników `grep`em po `[OK]`** — `test-auto-read.py` pisze `[OK ]` (ze spacją) i sumuje sam na końcu, więc wychodzi „0 OK, 0 FAIL", co czyta się jak pusta, bezwartościowa bramka. Rozstrzyga KOD WYJŚCIA (0/1) plus ostatnia linia podsumowania (realnie 22/0). Zmierzone 2026-08-18.

**Konfiguracja użytkownika — `~/.vibe-coding-assistant/`** (⚠️ po rebrandingu 1.0.25; `~/.claude-voice-assistant` to tylko ŹRÓDŁO MIGRACJI, `config._OLD_CONFIG_DIR`): `config.json` (język, głos, skin, `groq_api_key`, `auto_check_updates`, `claude_command`) · `agents.json` (+`splitter_sizes` per zakładka; jest `agents.json.autobak`) · `memory_projects.json` · `quick_actions.json` · `tts.log` · `login-events.log` · `webterminal.log` · `crash-logs/` · `cloud-*.json` (600).

**Zależności:** klient HTTP = **`requests`**, NIE httpx · **Groq = tylko STT** (wymaga klucza), **TTS = edge-tts, działa BEZ klucza** · `pyenchant` opcjonalny (`ENCHANT_AVAILABLE`) · `pygame.mixer.init()` w try/except (brak audio = TTS off, reszta działa).

---

## ⏳ CZEKA NA TEST NA ŻYWO

- 🔊 **EMOTIKONA PRZYKLEJONA DO SŁOWA BYŁA CZYTANA NA GŁOS** (`27955e3`, 2026-09-18, **NIEPRZETESTOWANE u usera** — pracował na becie, testuje 19.09). Zgłoszenie: „czyta emotikon smutek, kiedy widzi nawias i dwukropek"; polecenie objęło DWA kształty — `Gotowe :(` (działało) i `Gotowe:(` (**dziura**).
  - ⛔ **Czyścik emotikon ISTNIAŁ i działał — winny był jego strażnik `(?<!\w)`**, który wymaga braku litery przed dwukropkiem. Rozluźnić go NIE WOLNO (chroni `10:30`, `log:/tmp`), więc doszedł DRUGI, wąski wzorzec `_EMOTICONS_GLUED` tylko na przypadek przyklejony. **Nie scalaj tego w jeden wzorzec.**
  - ⭐ **DLACZEGO to boli — zmierzone u źródła** (edge-tts `pl-PL-ZofiaNeural`, rozmiar mp3 ∝ czas): cisza `0 B` · samo `:(` **12 816 B ≈ 1,3 s MOWY**. Emotikona to nie znaczek, tylko sekunda wypowiadanego tekstu. Technika reużywalna z tempa czytania.
  - ⛔ **CZEGO NIE WOLNO DODAĆ** (cena zmierzona na **8162** wypowiedziach z dzienników): `8)` → **139 kolizji** („art. 28)", „wersja 1.0.28)") · `:*` przyklejone → **13 kolizji** (pogrubienie „Powód:**…**") · `:/` → ścieżki i adresy · `:3` → godziny. Nowy wzorzec dał **0 trafień** na tych samych danych = nie tknął żadnego istniejącego zdania.
  - ⚠️ **ŹRÓDŁA NIE ZNALEZIONO W DZIENNIKACH** (0 przypadków na 8162) → może leżeć poza dziennikiem (ekran, zaznaczenie). Stąd **czujka `_audit_emoticons`** pisze do `tts.log` kształt + wąski kontekst, gdy do lektora trafi coś wymawialnego. **Przy nawrocie czytaj `tts.log`, nie zgaduj.**
  - ⛔ Wzorzec ma **DWIE kopie** (`text_cleaner` + `tts_engine`); kopia jest ŚWIADOMA (silnik bywa współdzielony między projektami — komentarz w pliku). Nie scalaj importem; sekcja D bramki pilnuje zgodności.
  - **Dowody:** `tools/test-emoticon-speech.py` **84/84** (mierzy SKUTEK) · `tools/sabotaz-emoticon-speech.py` **12/12 wykrytych**, 84 asercje w KAŻDYM przebiegu, przywracanie po `sha256` · regresja **29/29**.
  - ⚠️ **NAUKA O SOBIE:** dwie moje asercje były **PRZESTARZAŁĄ PREMISĄ, nie regresją** (żądałem, by lektor czytał adresy i `std::)` — stary kod robił to samo; rozstrzygnęło `git show HEAD`, nie intuicja). Pierwszy sabotaż dał 9/13 i w **3 z 4** przeoczeń winna była BRAMKA: przypadki testowe miały literę po nawiasie, więc `(?!\w)` i tak blokowało cięcie — asercje były prawdziwe niezależnie od kodu. Jeden guard okazał się **nieobserwowalny** → zapisany jako ZNANA GRANICA zamiast naciągania testu.
  - **Do testu po restarcie:** (a) `Nie wyszło:( trudno` → 🔊 milczy na emotikonie; (b) to samo z odstępem; (c) kontrola odwrotna — „10:30" i „art. 28)" DALEJ czytane; (d) regresja auto-czytania i „czytaj zaznaczenie".

- 🔴 **ZGŁOSZENIE OD AGENTA „Mieszkanie" (2026-09-06) — NOWA ZAKŁADKA WSTAJE BEZ `claude`. NIE RUSZANE, czeka na decyzję właściciela.** Kombinacja „otwórz od razu" + ODZNACZONY autostart daje zakładkę z samą powłoką: wpisane zdania lecą do basha (`Przeanalizuj: command not found`), **nic nie zgłasza błędu**. Przyczyna zmierzona w kodzie: `_add_agent` ustawia tylko `_run_immediately`, a `_run_agent` (przycisk ▶️) ustawia `_run_immediately` **ORAZ** `_force_start`; start bramkuje `main_window.py:1903`. Skala: **7 z 16 agentów** ma `auto_start: False`. **Do rozstrzygnięcia: czy „otwórz od razu" ma implikować uruchomienie Claude** (to zmiana znaczenia odznaczonego pola, nie kosmetyka) — albo ostrzeżenie w oknie dodawania, albo (⭐ leczy CAŁĄ klasę, także crash tuż po starcie) widoczny przycisk „▶️ Uruchom Claude", dopóki proces nie wstał. ⚠️ Kontrola odwrotna obowiązkowa: agent z `auto_start: False` otwierany PÓŹNIEJ **nadal NIE MOŻE** startować sam. 📄 Pełne zgłoszenie z cytatami kodu: `CLAUDE-VOICE-ASSISTANT-ARCHIVE.md`, sekcja „KONTRAKT OD AGENTA „Mieszkanie"".

- ✅ **TEMPO CZYTANIA 1× / 1,25× / 1,5× / 2× — POTWIERDZONE PRZEZ WŁAŚCICIELA NA ŻYWO 2026-09-18** („tempo działa"; `720339d`). Świadoma decyzja: **BEZ spowolnienia 0,5×**, cykl startuje od 1×.
  - ⭐ **Połowa mechanizmu LEŻAŁA W KODZIE OD ZAWSZE i nikt jej nie wołał:** `TTSEngine.set_rate()` przekazywał `rate` do `edge_tts.Communicate`, a `grep` dawał **0 wywołań** poza `tts_engine.py` (martwa była też stała `TTS_DEFAULT_RATE`). Brakowało wyłącznie drogi z GUI i zapisu ustawienia — stąd zmiana jest mała (214 linii w `src/`).
  - **ZMIERZONE, nie z cennika** (to samo zdanie, `pl-PL-ZofiaNeural`, rozmiar mp3 ∝ czas): `+0%`→46080 B · `+25%`→36864 B (0,80×) · `+50%`→30816 B (0,67× = 1,5× szybciej) · `+100%`→23184 B (0,50× = 2× szybciej). Mapowanie `+N%` → tempo (1+N/100)× zgadza się co do grosza. ⚠️ **Jedna próba `+50%` padła `NoAudioReceived` i przeszła 4/4 przy powtórce — to awaria PRZEJŚCIOWA usługi, NIE wada parametru.** Nie zapisz tego kiedyś jako „+50% nie działa"; ponowienia ma już `_generate_audio`.
  - ⛔ **KOMPROMIS, którego nie da się obejść: tempo łapie od NASTĘPNEGO zdania.** Audio powstaje na serwerze z „wypieczonym" tempem, a lektor pobiera kilka zdań do przodu (prefetch). Zdania trwają 1–3 s, więc różnica jest ledwo zauważalna. Przyspieszanie już pobranego nagrania na miejscu podniosłoby głos (efekt Myszki Miki) — `pygame.mixer` nie umie zmieniać tempa bez zmiany wysokości. **Powiedziane właścicielowi PRZED budową.**
  - **Gdzie co siedzi:** `config.TTS_RATE_LEVELS` = JEDYNE źródło poziomów (+ `tts_rate_label/next/multiplier`, separator dziesiętny za językiem: PL „1,25×", EN „1.25×") · `agent_tab.speed_btn` + sygnał `request_tts_speed` + `set_tts_speed_label()` **licząca szerokość z metryki czcionki** (etykiety mają różną długość; szerokość pod NAJDŁUŻSZĄ, żeby przycisk nie skakał) · `main_window`: `_cycle_tts_speed`/`_apply_tts_rate`, zapis `tts_rate` w `config.json` z **odczytem ŁAGODNYM** (wartość spoza listy nie dojedzie do lektora).
  - ⛔ **CZWARTE wystąpienie „pułapki białego kwadratu"** — `speed_btn` dopisany do `_apply_button_icon_styles`. Różni się od reszty tym, że niesie **NAPIS, nie ikonę**: stąd `font_size=14` zamiast 22 i stąd `color:` z arkusza realnie go maluje. Klucz koloru pożyczony od głośnika (`icon_read_color`) — nowy klucz skórki wymusiłby migrację ustawień. ⚠️ **W `_apply_skin_icons` go NIE MA i być nie może** (brak ikony do przemalowania) — nie „napraw" tego przez analogię do lupy.
  - **Dowody:** `test-tts-speed.py` 24/24 (z asercją na ŁADUNKU WYCHODZĄCYM) · sabotaż 14/14 wykrytych.
  - ⚠️ **NAUKA O WŁASNEJ BRAMCE:** sabotaż S2 najpierw nie wykrył NICZEGO — winna BRAMKA, nie kod: asercja pytała, czy w pliku STOI napis, a sabotaż zostawiał tę linię, psując `if`. Lek = **pomiar SKUTKU** (wywołaj prawdziwą funkcję na atrapie, odczytaj `styleSheet()`). ⭐ S5 dowiódł, że D1/D2 to NIE duplikaty — nie kasuj żadnej.

- 🎨 **SYGNAŁ PRZYCISKÓW PASKA PRZENIESIONY DO IKONY, 2026-09-15 — NIEPRZETESTOWANE u usera.**
  Zgłoszenie: „klikam przycisk, tło robi się fioletowe i ikona zostaje szara — ma być odwrotnie"
  oraz „X po wyczyszczeniu pola nie świeci się na czerwono".
  - **Obie usterki miały JEDEN mianownik: sygnał siedział w TLE i w RAMCE, a nie w ikonie.**
    Ikona zostawała szara, bo `color:` w arkuszu **nie dotyczy obrazka** (`QIcon`) — ta linia nie
    robiła nic od początku. Czerwony błysk DZIAŁAŁ, ale malował samą ramkę, a po kliknięciu kursor
    stoi na przycisku, więc reguła `:hover` przemalowywała ją na fiolet i przykrywała czerwień.
  - **Lek:** `_repaint_button_icon` przemalowuje IKONĘ (akcent przy „w użyciu", czerwień na błysk),
    tło zostaje spoczynkowe, a `border_override` wchodzi też do reguły `:hover`.
  - **Bramka:** `tools/test-bottom-bar-icons.py` **48/48** (nowe asercje mierzą PIKSELE ikony,
    nie arkusz) · sabotaż `tools/sabotaz-bottom-bar.py` **22 warianty, każdy wykryty, 48 WYKONANYCH
    w każdym przebiegu** (wyniki ZMIERZONE w nagłówku narzędzia). Regresja: 27/27 bramek zielonych.
  - ⛔ **Przy okazji znalezione i naprawione: cztery warianty sabotażu były MARTWE od 2026-09-14**
    (zdublowane klucze `B7`–`B10` w słowniku — Python zostawia ostatni, bez ostrzeżenia). Narzędzie
    sprawdza teraz własne źródło i odmawia pracy przy duplikacie.
  - **Do testu po restarcie:** (a) klik w spinacz/szybkie akcje/tryb myszy/lupę → **tło bez zmian,
    ikona fioletowa**, i gaśnie po zamknięciu menu/okna; (b) X po wyczyszczeniu pola → **ikona
    czerwona przez pół sekundy, także gdy trzymasz na nim mysz**; (c) kontrola odwrotna: kopiowanie
    dalej pokazuje ptaszka i zieloną ramkę; (d) mikrofon podczas nagrywania dalej czerwony.
  - ⚠️ **Znalezione, NIE naprawione (poza zakresem zgłoszenia):** lupa jest jedynym przyciskiem
    paska pominiętym w `_apply_skin_icons`, więc zmiana koloru ikon w skórce jej nie dotyczy.
    To ta sama rodzina co „każdy nowy przycisk dopisz w OBU miejscach". Do decyzji właściciela.

- 🆕 **NOWOŚCI CLAUDE CODE PRZENIESIONE DO VCA — CZTERY ETAPY, 2026-09-14, NIC NIE KLIKNIĘTE PRZEZ USERA.**
  Źródło: mail „This week in Claude Code" z 12.09. Commity: `b24bb48` (1), `61c6ec2` (2), `84e6ddc` (3), `3943b38` (4), `261a7d0` (kontrast).
  Wszystko wymaga RESTARTU bety — user pracował na niej cały dzień i nie mógł restartować.
  - **(1) Czujka modeli była MARTWA 27 DNI — to była najdroższa rzecz tej sesji.** Dostawca zaczął owijać identyfikatory w odwrotne apostrofy (`` `claude-fable-5-1` ``), `_strip_cell` ich nie zdejmował, wzorzec rodziny kotwiczy od POCZĄTKU napisu → 0 sparsowanych → `CatalogError` → fail-open → cisza. Skutek pieniężny: apka pisała „Fable 5", uruchamiając **Fable 5.1 (2× cena Opusa 5: 10/50 vs 5/25 USD za mln tokenów)**, a **2 z 16 agentów** usera na nim chodziło bez żadnej decyzji. ⛔ Bramka świeciła **61/61**, bo pytała WYŁĄCZNIE kopię strony z 26.07 → teraz DWA fixture (stary + dzisiejszy), oba obowiązkowo zielone. Doszło: ostrzeżenie o koszcie przy wyborze modelu (liczone z ceny w katalogu, nie wpisane), ślad po nieudanej próbie, okno po 30 dniach ciszy, próg zaufania do pliku podręcznego (30 dni).
  - **(2) Model podagentów per agent** (`CLAUDE_CODE_SUBAGENT_MODEL`, lista w oknie agenta) — rozmowa na Fable, podagenci na tańszym. **Poziom wysiłku per model** (`~/.claude/settings.json` → `modelSettings`), okno w Ustawieniach; zespół Claude Code zaleca dla Fable 5.1 `medium` zamiast `high`.
  - **(3) Lekarz skilli** (`claude -p /skill-doctor`, Ustawienia). ⭐ **ZMIERZONE U USERA: 23 skille = 2850 tokenów NA KAŻDĄ TURĘ, z czego 2120 marnowane na 16 nigdy nieużytych.** ⛔ Wyłączamy przez `skillOverrides`, NIE przez `permissions.deny` — zmierzone: `deny` blokuje uruchomienie, ale ZOSTAWIA pełny koszt (`Skill(pdf)` dalej ~150). Stany: `user-invocable-only` i `off` → koszt `-` (zero), `name-only` → z ~260 na <20, nietknięty → ~230 (kontrola). Domyślnie proponujemy `user-invocable-only`: user przestaje płacić i NIC nie traci (dalej wywoła `/nazwa`).
  - **(4) Hooki: pewna wiedza o modelu + wznawianie rozmów.** Zmierzone ładunki (fixture `tools/fixtures/hook-events-2026-09-14.jsonl`): `PostModelSwitch` → `from_model`/`to_model`/`requested_model`/`source`; `SessionStart` → `session_id`/`transcript_path`/`cwd`/`source`, **pole `model` BYWA POMINIĘTE** (u nas go nie było) → model bierzemy z `PostModelSwitch`, dziennik zostaje drugą drogą. ⭐ `from_model` przy pierwszym przełączeniu niesie model, na którym sesja RUSZYŁA — odpowiedź na „co uruchomił Domyślny". ⚠️ Identyfikator przychodzi z dopiskiem okna (`claude-opus-5[1m]`) — bez zdjęcia go mapa `CLAUDE_MODEL_API_IDS` nie trafia. Hook ma pierwszeństwo nad dziennikiem dla TEJ sesji (inaczej pasek miga). ⛔ Hooki wpinamy WŁASNYM plikiem przez `--settings`, NIE do `~/.claude/settings.json` — zmierzone, że taki plik **DOKŁADA SIĘ** do ustawień usera, a nie zastępuje ich; zasięg = tylko zakładki VCA. ⭐ `--resume <id>` **zachowuje TEN SAM identyfikator sesji** (`source=resume`), więc czytnik dziennika zostaje przypięty — stąd wznawianie jest bezpieczne dla 🔊 i flagi. Rejestr proponuje WYŁĄCZNIE sesje z ISTNIEJĄCYM dziennikiem.
  - **(5) Kontrast dwóch sygnałów** (`261a7d0`): wybrana zakładka `#251c37`→`#3a2d57` (+26→+49), panel nieaktywnego okna `#2b2438`→`#413659` (+31→+55). ⛔ Oba MIEŚCIŁY SIĘ w regule ≥20/255 i user i tak zgłosił oba jako niewidoczne → **na tle o jasności 14/255 próg 20 jest podłogą, nie gwarancją**; te dwa mają teraz własny próg 40 (asercje 3h–3k). **SKIN_VERSION 5 → 6** — bez tego zmiana byłaby NIEWIDOCZNA (config przykrywa paletę); sprawdzone przed podbiciem: **40/40 kolorów domyślnych, zero własnych**, migracja nic nie kasuje.
  - **DO TESTU PO RESTARCIE (12 punktów):** (a) lista modeli pokazuje „Fable 5.1"; (b) przy Fable widać „2× droższy niż Opus 5", przy Opusie napis znika; (c) pasek mówi „Zaktualizowano nazwy modeli: Fable 5 → Fable 5.1"; (d) „Sprawdź nowe modele" → lista aktualna; (e) licznik tokenów i nazwa modelu jak dotąd; (f) „Model podagentów" zapisuje się i pamięta; (g) „Poziom wysiłku modeli" zapisuje, a inne ustawienia Claude Code zostają; (h) „Lekarz skilli" → raport, „zaznacz nieużywane", zapis; wyłączony skill DALEJ działa po wpisaniu `/nazwa`; (i) `/model sonnet` → pasek od razu pokazuje Sonnet; (j) restart apki → pytanie „Wznowić poprzednie rozmowy?", na TAK agent pamięta kontekst; (k) na NIE nic nie ginie z historii; (l) klik poza okno → panel wyraźnie jaśnieje, zakładka odcina się od reszty.
  - **Bramki:** `test-model-catalog` 111/0 · `test-subagent-effort` 73/0 · `test-skill-doctor` 76/0 · `test-hooks-resume` 64/0 · `test-bottom-bar-icons` 41/0. Sabotażyści: 9, 9, 10, 10, +4 warianty. Razem 27 bramek projektu zielonych.
  - ⚠️ **NAUKA O SOBIE, warta przeczytania przed pisaniem następnej bramki:** na 33 warianty sabotażu **siedem** za pierwszym razem nic nie wykryło, a w **sześciu** winna była BRAMKA, nie kod — same pułapki opisane w `CLAUDE-COMMON.md`, zaliczone MIMO przeczytania go na starcie. Szczegóły w nagłówkach sabotażystów (są tam ZMIERZONE tabelki).


- 🎤 **DYKTOWANIE „DZIURAWE" — CAŁA DROGA TEKSTU JEST ZDROWA, WINA W ROZPOZNAWANIU** (`f79b1da`, 2026-09-11, **NIEPRZETESTOWANE u usera** — beta chodziła ze starym kodem, user nie mógł jej restartować). Zgłoszenie: „tekst nie jest tym, co dyktuję; w zakładce AI Manager musiałem bardzo dużo naprawiać".
  - ⛔ **ZMIERZONE, ZANIM COKOLWIEK ZMIENIONO — nie powtarzaj tych sond:** 328/328 nagrań ma liczbę ramek **zgodną z zegarem** (nagrywanie nic nie gubi; ⚠️ „dlugosc" w logu jest WYLICZANA z ramek, więc sama w sobie niczego nie dowodzi — rozstrzyga dopiero zestawienie z czasem między kliknięciami) · długość odpowiedzi bramki == długość wstawiona do pola · **325/325** dyktowań trafiło we właściwą zakładkę · brak sufitu długości (najdłuższe nagranie **193,7 s**) · tekst dociera do Claude Code z ogonkami — **to NIE jest powrót lipcowego buga z polskimi znakami**.
  - ⛔ **OBALONE POMIAREM (każda próba kosztuje wywołanie bramki — nie powtarzaj):** `language=pl` → wynik **co do znaku identyczny** z „auto" (⭐ kontrola przytomności: `language=de` zwrócił niemiecki, więc parametr NAPRAWDĘ dociera) · podpowiedź słownictwa (`prompt`) i `temperature=0` → **bez zmian** · ciche wzmocnienie mikrofonu (u usera **21%**) → ściszenie wzorca do 21% i 10% **nie zmieniło zgodności** (97,4%), bo rozpoznawanie samo normalizuje głośność · `whisper-large-v3-turbo` → **GORSZY** (95,2% wobec 97,4%).
  - **Sufit jest w modelu:** na nagraniu wzorcowym o znanej treści, czystym studyjnie, Whisper i tak przekręca `klucz`→„kruć", `usuń`→„usun", `zepsuł`→„cepsuł". Te same dwie klasy błędów (ogonki + słowa z dziedziny) widać w prawdziwych dyktowaniach usera.
  - **Lek: poprawianie transkrypcji po fakcie** przez tę samą bramkę (`STT_FIX_*` w `config.py`, `_popraw_transkrypcje` w `stt_engine.py`). Na wzorcu **97,4% → 98,5–100%**; na PRAWDZIWYM tekście usera naprawiło `kazda`→`każdą`, `usun`→`usuń`, `daje`→`Daj` i dostawiło kropki, **nie ruszając jego sformułowań**.
  - ⭐ **MODEL WYBRANY POMIAREM, nie z katalogu** — szybkość i wierność okazały się NIEZALEŻNE: `gemini-3.5-flash-lite` **1,0–1,5 s, zachowuje 5/5 własnych sformułowań** (WYBRANY) · `gemini-3.6-flash` wierny, ale mediana **8,5 s**, raz 18,3 s · `gpt-oss-120b` szybki i **PRZEPISUJE wypowiedź** („daj mi"→„pokaż", „wyrzuć mi"→„usuń") · `gpt-oss-20b` oddaje **pustkę**.
  - ⚠️ **Próg czasu dobrany na krótkiej próbce ODCINAŁ uczciwą pracę po cichu:** pierwsze 8 s (z pomiaru na jednym zdaniu) dawało `ReadTimeout` na prawdziwym tekście 524 znaki → cichy powrót do tekstu surowego. Teraz **12 s** przy zmierzonym najgorszym przypadku 8,8 s.
  - **Bezpieczniki:** wyłączone / brak klucza / awaria sieci / kod ≠ 200 / pustka / stosunek długości poza 0,80–1,30 → **oddajemy tekst SUROWY**, nigdy pustkę; druga kontrola porzuconego podejścia PO poprawce. ⚠️ **Czego bezpiecznik NIE łapie: PRZEPISANIA** — parafraza zmieściła się w widełkach, a podobieństwo znakowe nie rozróżnia (wierny 90,5%, parafrazujący **95,1%**, czyli WIĘCEJ). Chroni WYBÓR MODELU, nie próg.
  - **Przełącznik:** `stt_fix_enabled` w `config.json`. ⛔ **Checkboxa w Ustawieniach NIE MA — świadoma decyzja właściciela 2026-09-11** („nie"). Nie dokładaj go bez pytania; wyłączenie odbywa się przez prośbę do agenta.
  - **Bramki:** `tools/test-dictation-fix.py` **30/30** (w tym sekcja pilnująca, CO wychodzi na sieć — adres, model, polecenie, temperatura, klucz; to ona zamyka lukę „żaden test nie sprawdzał modelu dyktowania") + `tools/sabotaz-dictation-fix.py` **11/11 wykrytych**, wyniki ZMIERZONE w nagłówku. Regresja 24/24.
  - **Do testu po restarcie:** (a) ogonki w dyktowaniu; (b) żargon — własne słowa mają zostać; (c) **zaznacz tekst w polu i podyktuj — zaznaczenie NIE MOŻE zniknąć**; (d) bez sieci → tekst surowy, nie pustka; (e) regresja 🔊 i auto-czytania.
  - ⏳ **Krok A czeka na odczyt:** dziennik notuje teraz `pole: znakow=… kursor=… zaznaczone=…`. Po kilku dniach pracy usera **odczytaj z `dictation.log`, czy ochrona zaznaczenia w ogóle się uruchamia** — dziś to hipoteza z czytania kodu, nie pomiar (różnicę 689→524 znaków z 2026-09-11 user wyjaśnił: **poprawiał ręcznie**).

- 🎤 **DYKTOWANIE: mikrofon milkł NA STAŁE, a błąd był NIEWIDZIALNY** (`78a3ef6`, 2026-08-29). Zgłoszenie: „dyktuję, tekst nie wchodzi w pole poleceń; **ikona nawet nie pulsowała**" — brak pulsowania = nagrywanie w ogóle nie ruszało. ⛔ **Zanim ruszysz ten temat: przeczytaj `~/.vibe-coding-assistant/dictation.log`** — od tej poprawki dyktowanie w końcu coś zapisuje.
  - **Wykluczone POMIAREM** (nie domysłem): mikrofon OK (24 ramki, głośność 153) · bramka STT OK (200 w 1,2 s, tekst poprawny) · RAM 7,3 GB wolne, swap 0 · żadna świeża poprawka nie dotykała dyktowania · zero wyjątków w dzienniku systemowym. **Nie powtarzaj tych sond.**
  - **Przyczyna:** apka utknęła w stanie „przetwarzam", bo wysyłka nie wróciła — **limit `requests` NIE obejmuje zamiany nazwy na adres**, a przy zerwanym Wi-Fi `getaddrinfo` wisi dłużej. W tym stanie `start_recording()` wychodził po cichu → każde kolejne kliknięcie ginęło. Zmierzone na produkcyjnym silniku: klik 1 → nic, klik 2 → nic, klik 3 → nic. Tło dnia: Wi-Fi rozłączyło się 12:37, DNS „przycięty" 4×, lektor 7× stracił połączenie.
  - ⛔ **DRUGA, NIEZALEŻNA WADA — i to ona ukrywała pierwszą przez cały czas:** komunikat o błędzie był NADPISYWANY słowem „Gotowy" w tej samej milisekundzie (`on_error` → pasek, zaraz potem `_set_state(IDLE)` → pasek „Gotowy"). W trybie zakładek `conversation_area` jest `None`, więc `_append_system_message` spada do paska stanu. **Reguła na przyszłość: komunikat na pasku stanu NIE JEST komunikatem — sprawdź, co go nadpisze w następnej milisekundzie.**
  - Lek: (1) `dictation.log` ZAWSZE włączony, bez czujnika (ikona z pulpitu = włącznik środowiskowy bywa martwy; limit 512 KB; bez klucza i bez pełnej wypowiedzi); (2) `decide_mic_click()` — decyzja wyjęta z okna do czystej funkcji, bo warunek inline nie ma czego odpytać; (3) błąd zostaje na ekranie (chorągiewka + niemodalne okno, raz na 5 min); (4) limit 30 s → 12 s, próg zakleszczenia 15 s, straż numeru podejścia (porzucony wynik nie wpada do pola pół minuty później).
  - **Dowody:** `tools/test-dictation.py` **48/48**, sabotaż **9/9 wykrytych** (wyniki ZMIERZONE w nagłówku), 21/21 bramek projektu, 4 gałęzie okna wykonane na produkcyjnym kodzie + przebieg końca-końca przez żywą bramkę.
  - ⚠️ **Sabotaż obnażył dziurę we WŁASNEJ bramce:** asercja „klucz nie wycieka" badała okno logu, w którym klucza nie ma prawa być → przechodziła na pustce (złapało go przypadkiem B5). Stąd sonda C0 wymuszająca linię dotykającą klucza.
  - ⚠️ **Dwie pułapki zaliczone przy pisaniu testu:** polski cudzysłów `„` domknięty ASCII-owym `"` urwał łańcuch; a podstawienie `__file__` jako atrapy nagrania sprawiło, że **test SKASOWAŁ SAM SIEBIE** (`_transcribe_audio` kasuje plik z `_save_wav`). Trzecia: test gałęzi GUI bez podmiany `HOME` **zatruł prawdziwy `dictation.log`** atrapami — sprzątnięte ręcznie, kopia w scratchpadzie.
  - **Do testu (wymaga restartu bety):** (a) normalne dyktowanie; (b) klik od razu drugi raz → „Przetwarzam poprzednie nagranie" + przycisk wraca; (c) Wi-Fi wyłączone → okno „Dyktowanie nie doszło", nie cisza; (d) gdyby mikrofon znów zamilkł → kolejny klik po 15 s ma odblokować.

- ✅ **WYDANE w 1.0.29 (2026-09-03) — te dziewięć poprawek NIE czeka już na paczkę.** Kanał przełączony na 3 platformy, sumy kontrolne zgodne u odbiorcy, obecność poprawek udowodniona W PACZCE (PYZ AppImage: `tables_to_prose`, `CODE_FENCE_RE`, `decide_mic_click`, `repair_terminal_mojibake`, `claude_binary_health` — 5/5). Droga powrotu: `appcast.json.bak-1.0.28` na VPS. 🧹 Diagnostyka 🔊 pojechała dalej (znacznik `read-last-debug.on` u usera ISTNIEJE) — dług do sprzątnięcia zostaje.
- 🔊 **PLAN B LEKTORA ODDAWAŁ 0 ZNAKÓW — 🔊 milczał MIMO działającej poprawki 209de99** (`359fa8c`, 2026-09-04, ZACOMMITOWANE, **NIEPRZETESTOWANE u usera**). Zgłoszenie: klik 🔊 w zakładce SEO M przy pytaniu z polami wyboru, „zero reakcji”. ⛔ **Beta MIAŁA kod** (start 08:45 vs poprawka z 28.08) — przyczyna (1) z drzewa sprawdzona PIERWSZA i odrzucona. Log rozstrzygnął od razu: `DZIENNIK ZAMROZONY 762s -> probuje EKRANU` ✅ → `EKRAN: nic nie wylowiono — NIE CZYTAM` ❌ → `WAIT` → cisza.
  - ⛔ **Błąd był PROJEKTOWY, nie wykonawczy: przy zamrożonym dzienniku kotwica Z DZIENNIKA jest z definicji PRZETERMINOWANA** (pochodzi sprzed zamrożenia), a im dłużej ono trwa, tym pewniej wyjechała poza ekran. Zmierzone: ostatni wpis 09:14, klik 10:27:50 = **kotwica o 73 min starsza** niż bufor ekranu, który i tak jest cięty do **5000 znaków** (`agent_tab.py`). Plan B opierał się WYŁĄCZNIE na niej.
  - **Lek, 4 warstwy:** (1) NOWE ŹRÓDŁO — `window.__termScreenText` czyta `term.buffer.active`, czyli **wyrenderowaną siatkę** (bez ANSI, bez mojibake, bez duchów po przerysowaniach TUI); mostek jak w działającym `__termScrollTo`, a że odczyt wraca callbackiem, migawkę podgrzewa `_poll_transcripts` TYLKO gdy agent czeka na usera; (2) **TOR 3 „ogon ekranu”** — brak kotwicy nie może już znaczyć ciszy; (3) naprawa kodowania **PRZED** porównaniem kotwicy; (4) `strip_tui_noise()` — JEDNO źródło wzorca ozdób TUI (bramka pilnuje, by nikt nie wkleił go inline).
  - ⭐ **Znalezione poza zgłoszeniem: `extract_last_claude_response` oddawał 79 zn. z 490** — łapał ramkę **WIDGETU PYTANIA**, nie wypowiedzi. Lektor przeczytałby ⅙ treści = nowa usterka. Stąd **próg współmierności** (→ 490/490). Ekstraktor bywa nie tylko pusty, ale **mylnie NIEPUSTY**.
  - ⚠️ **GRANICA METODY, zapisana jawną asercją — nie łataj jej w filtrze:** w starym strumieniu PTY spinner jest POSZATKOWANY na pojedyncze znaki w setkach linii (`W`, `o`, `r`, `✻Wk`), więc filtr LINIOWY go nie posklei. To powód zmiany ŹRÓDŁA, nie wada filtra.
  - **Dowody:** `tools/test-read-last-screen.py` **43/0**; `tools/sabotaz-read-last-screen.py` — 7 wariantów, każdy wykryty (ZMIERZONE: 2/1/2/11/10/2/1); regresja **17/17**. ⚠️ Sabotaż obnażył dziurę we WŁASNEJ bramce: asercja o mostku JS pytała o PODCIĄG nazwy → **0 zapalonych** nad usuniętą funkcją. ⚠️ `test-read-last-{wait,blocked}` pożyczają metody produkcji PO NAZWIE → nowa zależność (`_screen_source`) psuje je i wygląda jak regresja produkcji; A1 było PRZESTARZAŁĄ PREMISĄ (etykieta toru niesie teraz źródło: `kotwica [siatka]`).
  - **Do testu po restarcie:** (a) pytanie z polami wyboru + 🔊 → czyta wypowiedź Z OPCJAMI; (b) kontrola odwrotna: zwykła odpowiedź → czyta z dziennika; (c) czy nie czyta na głos „Working, 12 minut, 22 tysiące tokenów”; (d) dwa kliknięcia pod rząd; (e) regresja auto-czytania i „czytaj zaznaczenie”. → `read-last-pytanie-polami-wyboru.md`
- 🗓️ **TRZY BUGI LEKTORA CZEKAJĄ NA JEDEN RESTART — TEST 2026-08-29.** ⚠️ Termin 2026-08-27 MINĄŁ NIEWYKORZYSTANY: user PRACUJE na becie i nie może jej restartować na żądanie, a bez restartu stary czyścik siedzi w pamięci. Nie planuj testu „na jutro” — pytaj, KIEDY user może zrestartować.
  - **(a) POTRÓJNY ZNACZNIK** (`fc4a1ca`, naprawione 2026-08-26). User nie mógł testować tego samego dnia. ⚠️ Do testu potrzebny **restart bety** — działająca apka ma stary czyścik wczytany w pamięci. Scenariusz: (1) 🔊 na odpowiedzi, w której agent PISZE o znacznikach ``` w zdaniu — ma doczytać do końca; (2) **kontrola odwrotna:** odpowiedź z PRAWDZIWYM blokiem kodu — lektor NIE MOŻE czytać kodu; (3) auto-czytanie dłuższej odpowiedzi; (4) lupa → „przeczytaj fragment"; (5) „czytaj zaznaczenie" (druga naprawiona droga).
  - **(b) CAŁE TABELE KASOWANE** (`2d4a163`, naprawione 2026-08-28; zgłoszenie usera tego ranka: „wycięło mi większość rzeczy, czyta tylko początek i koniec, środek wycina”). ⛔ **TEN SAM OBJAW CO (a), INNA PRZYCZYNA — nie pomyl ich:** apka MIAŁA `fc4a1ca` załadowaną (proces wstał 08:04, poprawka z 26.08 10:29), więc „to nawrót” było fałszywym tropem, obalonym porównaniem czasu startu procesu z datą commita. Realnie: wszystkie trzy czyściki kasowały każdy wiersz `|...|` w CAŁOŚCI. **Zmierzone:** zgłoszona wypowiedź 2361 → **703 zn. (30%)**, po naprawie 2135 (90%); na produkcji **322 z 565** długich wypowiedzi (**57%**) zawiera tabelę, średnio docierało 73% treści, najgorsze 23–30%. Lek: tabela → zdania „Nagłówek: wartość” (wariant wybrany przez usera z trzech). Wzorzec ZNÓW stał inline w 3 kopiach → jedno źródło `tables_to_prose()` + bramka `tools/test-table-prose.py` (27/0, fixture z prawdziwego dziennika).
    - ⭐ **Sabotaż (7 wariantów, 6 wykrytych) obnażył słabości WŁASNEJ bramki:** C1–C3 przechodziły na surowych kreskach (badały, czy słowo przeżyło, a nie czy zamieniono je na zdanie), B5/B7 mierzyły koniec potoku zamiast jednostki. ⚠️ Narzędziowo najważniejsze: **`__pycache__` podsuwał STARY bytecode** wariantom dającym plik tej samej długości w tej samej sekundzie → trzy różne sabotaże raportowały identyczny wynik, co wyglądało jak sprzężenie dróg czytania. Sabotaż Pythona uruchamiaj z `-B` i kasuj `__pycache__`.
    - ⚠️ Wariant 5 (kolejność wobec bloków kodu) **NIE jest wykrywany** — ramka bloku kasuje zawartość niezależnie od kolejności. Zapisane w kodzie jako fakt, nie łatane na siłę; nie licz na test, którego nie ma.
  - **(c) 🔊 MILCZY, GDY NA DOLE WISI PYTANIE Z POLAMI WYBORU** (`209de99`, naprawione 2026-08-28 — zgłoszenie usera: „kilka razy w kilku zakładkach"). ⛔ **Claude Code 2.1.250 NIE ZAPISUJE wypowiedzi do dziennika, dopóki pytanie czeka na odpowiedź** — cały blok (tekst ORAZ pytanie) ląduje w pliku po kliknięciu. Zmierzone: wpisy ze znacznikiem 08:27 trafiły do pliku po 09:00, **plik zamrożony 32 min**. 🔊 widzi wtedy STARĄ krótką wypowiedź + `owes_text` → czeka → po 30 s „agent stanął" → milczy. **Skala: 26 z 28 nieudanych kliknięć (93%) miało wiszące pytanie; kontrola negatywna 5 z 83 udanych (6%).**
    - ⛔ **OBALONA TEZA, KTÓRA STAŁA W KODZIE:** komentarz w `main_window` twierdził (sonda PTY na **2.1.212**), że „nowszy Claude Code zapisuje OD RAZU" i odraczanie to przeszłość. Na **2.1.250 to nieprawda** — sprostowane w kodzie. Przy każdym „przecież sprawdzaliśmy" pytaj, NA JAKIEJ WERSJI.
    - Lek (oba warianty wybrane przez usera): (1) poddanie się czekania ma PLAN B — czyta z EKRANU, **z opcjami pytania włącznie**; (2) dziennik zamrożony ≥120 s przy `owes_text` → nie czekamy wcale (próg z pomiaru: najdłuższa przerwa „wynik narzędzia → tekst" ~71 s; złapałoby 18 z 28 od razu); (3) bufor ekranu NIE jest już czyszczony po czytaniu.
    - ⛔ **`extract_last_claude_response` NIE WYSTARCZA** — szuka RAMEK okna, a te bywają rozsypane przez kodowanie: na prawdziwym zrzucie z `crash-logs/` zwraca **0 znaków**, więc naprawa na nim oparta **milczałaby po cichu**. Stąd kotwiczenie się w tekście znanym z dziennika i branie wszystkiego po nim. Gdyby ktoś chciał to „uprościć" — patrz asercja A3 w bramce.
    - ⚠️ `fix_polish_encoding` **nie naprawi** rozsypanego bufora terminala: odrzuca naprawę bezpiecznikiem „nie strać >10% znaków", a naprawa mojibake z definicji SKRACA (792→550 zn.). Stąd `repair_terminal_mojibake()` z kryterium „treść ASCII przetrwała ≥98%" (kontrola negatywna: poprawny polski tekst nietknięty).
    - Bramka `tools/test-read-last-blocked.py` 25/0, sabotaż **7/7 wykrytych**, fixture = PRAWDZIWY zrzut terminala z pudełkiem wyboru.
  - **Co było zepsute w (a):** czyścik parował ramki bloków kodu NA ŚLEPO, więc gdy agent o nich PISAŁ („owinięty w znaczniki ```"), parowanie przesuwało się o jeden. Lek: ramka WYŁĄCZNIE na początku linii (tak działa Markdown) + zdjęcie osieroconych znaczników + sprzątanie spacji przed interpunkcją.
  - ⛔ **KOREKTA mechanizmu — objaw ma DWIE postacie, a notatka znała jedną.** Odnalazłem oryginalną wypowiedź ze zgłoszenia (2026-08-11 08:52, AI Manager, 3035 zn., 5 znaczników, tylko 2 na początku linii) i puściłem przez STARY kod: **nie urwał jej na końcu, tylko POŁKNĄŁ ~950 zn. ZE ŚRODKA**, a zakończenie przeczytał w całości. Wersja „urywa na końcu" jest GŁOŚNA, wersja „połyka środek" CICHA — więc „doczytał do końca" NIE dowodzi, że nic nie zginęło.
  - **Skala na produkcji** (316 wypowiedzi ≥400 zn. z dzienników): **8 okaleczonych (2,5%)**, średnio do lektora docierało **50% treści → teraz 75%**; najgorszy przypadek 3743 zn.: 38% → 74%.
  - ⭐ **Naprawione WSZYSTKIE TRZY czyściki, bo wzorzec był WKLEJONY INLINE w trzech kopiach** (`prose_from_markdown` = 🔊/auto-czytanie/lupa · `TextCleanerForTTS` = „czytaj zaznaczenie" · `extract_last_claude_response` = odczyt z ekranu). Naprawa jednej zostawiłaby dwie zepsute — dlatego wzorzec stoi teraz RAZ jako `CODE_FENCE_RE`/`ORPHAN_FENCE_RE`, a bramka pilnuje (F11), że nikt go z powrotem nie wklei inline. Siostrzane czyściki nie miały reguły „wytnij do końca", więc miały wyłącznie wariant CICHY: z „owija w ``` a potem ``` i jeszcze ```" ginęło „a potem".
  - **Dowody:** bramka `tools/test-prose-blocks.py` **44/0**, sabotaż **8 wariantów, każdy wykryty** (wyniki ZMIERZONE w nagłówku bramki, przywracanie z pamięci procesu + sha256). Regresje zielone: `test-auto-read` · `test-read-last-wait` · `test-conversation-search` · `test-cleanup`. → `tts-proza-urwana-potrojny-znacznik.md`
- 🎨 **IKONY PASKA W JEDNEJ TONACJI + kropka auto-czytania** (`3aafffa`, 2026-08-28) — user: „mikrofon, X i błyskawica są bardziej białe niż pozostałe". ⛔ **Przyczyna NIE była w rysunkach:** wyrenderowane w tym samym kolorze mają IDENTYCZNĄ jasność (157), a mikrofon i błyskawica nie są nawet najcięższe (najwięcej atramentu ma spinacz, 24,3%). Różnicę robił WYŁĄCZNIE kolor w skórce: cztery klucze stały na `TEXT`, reszta na `TEXT_DIM`. Odrzucone tropy: „SVG mają wklejony kolor" (nie — wszystkie 18 ma `currentColor` ×1) i „błyskawica to wypełnienie, nie kreska" (prawda, jedyna z `fill=currentColor`, ale przy tym samym kolorze świeci tak samo).
  - Lek: `icon_dictate/clear_input/quick_actions/search` → `TEXT_DIM`; gałka przełącznika auto-czytania wyłączony `#e6e2ee`→`#9b93a8`, włączony `#ffffff`→`#eae6f2`. ⚠️ Włączonego **świadomie NIE** zeszliśmy do `TEXT_DIM`, choć user prosił o jedną tonację: na fioletowym torze różnica to **10/255**, poniżej progu 20 dla odcienia niosącego SYGNAŁ — kropka zlałaby się z tłem. Powiedziane userowi wprost.
  - ⭐ **Lupa doszła z własnej inicjatywy** — user jej nie zgłosił, ale stała na tej samej prawie bieli i zostałaby jedyna jasna, czyli byłoby to kolejne zgłoszenie.
  - **`SKIN_VERSION` 4 → 5.** Podbicie KONIECZNE (bez niego zapisane kolory przykrywają nowe = zero widocznej zmiany). ⭐ Przepis, ile kosztuje, SPRAWDZONY PRZED podbiciem: porównaj `config.json` → `skin_colors` z `DEFAULT_SKIN_COLORS` — u usera 40/40 identycznych, zero własnych; `skin_icons` (własne napisy) i tak nie podlegają migracji. ⚠️ Kolory siedzą ZAGNIEŻDŻONE w `skin_colors`, nie na najwyższym poziomie — moja pierwsza sonda patrzyła na zły poziom i orzekła „0 zapisanych kolorów", co o mało nie zwolniło mnie z podbicia.
  - Bramka `tools/test-bottom-bar-icons.py` 6 → 12 asercji (reguła jednej tonacji + kontrast obu gałek z kontrolą odwrotną), sabotaż 6/6. ⚠️ Asercja „lupa MA być bielą" była PRZESTARZAŁĄ PREMISĄ, nie regresją — zastąpiona regułą mocniejszą, wyjątki znaczeniowe wymienione z nazwy.
- **Serwery MCP** — nigdy niesprawdzone na żywo (dodawanie/usuwanie, gating per agent, licznik i status w pasku). Zaległość zgłoszona 2026-07-25.
- **Ładowanie agenta z CHMURY** na drugim komputerze — kod Fazy 1 sprawdzony na atrapie i na prawdziwym Dysku Google, ale kliencka droga „pobierz i pracuj" nieprzeszła. → `chmura-sync-agentow.md`
- **Wyścig „/login"** (`e6af2c3`) — mechanizm ŻYJE (pierwszy wpis w `login-events.log` 2026-07-23), ale to był błąd SIECI (`ENOTIMP`), więc werdykt „wyścig vs prawdziwe wylogowanie" wciąż niewywołany.
- **KLIENCKI self-update na LINUKSIE (AppImage)** — Mac potwierdzony 2026-08-07 (patrz niżej), Windows i Linux wciąż bez potwierdzenia klienckiego cyklu. Cofnięcie kanału gdyby trzeba: `cp appcast.json.bak-1.0.27 appcast.json` na VPS.
- **Auto-czytanie po auto-compact** (`1b57c60`) — najstarsza niepotwierdzona rzecz. ⚠️ NIE mylić z „auto-czytanie działa" (potwierdzone): tu chodzi wyłącznie o zachowanie po tym, jak Claude Code sam skróci dziennik (plik maleje poniżej offsetu; stary kod ustawiał `offset=0` → recytacja od początku). → `auto-czytanie-loop-po-kompaktowaniu.md`

⚠️ **Zanim zdiagnozujesz „fix nie działa" — sprawdź, czy beta w ogóle ma ten kod:** `ps -o lstart= -p $(pgrep -f '[s]rc/main.py')` vs `git log -1 --format=%ad <commit>`. Dwa razy uratowało to przed szukaniem błędu w kodzie, którego apka nie miała. Rzeczy niewywoływalne na życzenie (wyścig /login, auto-compact) czekają na okazję, nie na chwilę usera. ⛔ **I SPRAWDŹ TO TAKŻE W DRUGĄ STRONĘ, ZANIM POWIESZ WŁAŚCICIELOWI, ŻE COŚ „CZEKA NA RESTART" — ten wpis starzeje się SAM, bez niczyjej decyzji.** Wystarczy, że user zamknął i otworzył apkę rano. Zmierzone 2026-09-15: pamięć niosła „wszystko wymaga restartu bety, user nie mógł restartować", a proces bety wstał **tego dnia o 08:39**, czyli PO wszystkich poprawkach z 14.09 — komplet z 12 punktami dało się kliknąć od razu. Gdybym powtórzył wpis bez pomiaru, właściciel dostałby listę zadań zablokowaną warunkiem, który już nie obowiązywał. Ta sama komenda rozstrzyga oba kierunki: `ps -o lstart= -p $(pgrep -f '[s]rc/main.py')` wobec `git log -1 --format=%ad <commit>`. ⚠️ Przy dwóch apkach na tej samej nazwie procesu rozstrzyga KATALOG ROBOCZY (`readlink /proc/<pid>/cwd`), nie nazwa.

- 🖥️ **OKNO ZA DUŻE NA MAŁYM EKRANIE WINDOWS — dwie przyczyny naraz** (`74413f6`, 2026-09-03; **nie ma tego w 1.0.29**, wejdzie w 1.0.30). Zgłoszenie z komputera współpracownika: „wszystko powiększone, nie widać dołu", a **maksymalizacja NIE POMAGAŁA**. ⛔ To NIE była czcionka: (1) Qt5 zaokrągla skalę W GÓRĘ, więc Windows **150% było rysowane jako 200%**; (2) `setMinimumSize(900,650)` wpisane NA SZTYWNO było WIĘKSZE niż ekran — i to ono blokowało maksymalizację. **Zmierzone: okno 1800×1300 px na ekranie 1366×768 (≈2,5× powierzchni ekranu), user widział ⅓ programu.** ⭐ **Żadna poprawka sama nie wystarcza** (sama (1) → minimum 650 dalej nie mieści się w 485; sama (2) → wszystko nadal w 200%). Decyzja wyjęta do czystej funkcji `startup_geometry_for()`. **Na ekranie 1920×1080 wartości IDENTYCZNE jak dotąd — zmierzone, to ochrona przed regresją u właściciela.** Bramka `tools/test-window-geometry.py` 25/0, sabotaż 5/5 (`tools/sabotaz-window-geometry.py`, tryb `--kotwice`). ⚠️ **Wymaganie właściciela: skala systemu to ustawienie DOSTĘPNOŚCI dla osób słabiej widzących — program ma się dostosować, nigdy nie wychodzić poza ekran ani chować dolnego paska.** → `CLAUDE-COMMON-DESKTOP.md`
- 🪟 **INSTALACJA NA WINDOWS OD ZERA — przebieg zmierzony u współpracownika 2026-09-03** (Windows 10, Dell 1366×768, skala 150%). Kolejność przeszkód, każda wyglądała jak awaria naszego programu: `npm install` odbite przez blokadę skryptów PowerShella → instalator natywny `irm https://claude.ai/install.ps1 | iex` **przechodzi mimo blokady** → program ląduje w `~\.local\bin`, **którego NIE MA w PATH** → martwe skróty po npm (`claude`, `claude.cmd` wskazujące w pustkę) **przesłaniają** sprawną wersję → VCA sam wpisał sobie do `config.json` ten martwy skrót → biały ekran przy starcie (kreator czekał POD głównym oknem) → i na końcu: **konto darmowe, a Claude Code wymaga płatnego planu**. Lek u usera: Ustawienia → Komenda Claude Code = PEŁNA ŚCIEŻKA `C:\Users\<user>\.local\bin\claude.exe`. → `windows-claude-niezgodny-npm.md`

## AKTUALNY STAN — wersja 1.0.29 (wydana 2026-09-03, 3 platformy)

- **1.0.29** — wydanie zbiorcze 9 poprawek gotowych od 1.0.28 (crash Maca przy zamykaniu, 🔊 runda 6, trzy usterki lektora, dyktowanie, ikony paska, rozpoznanie uszkodzonego Claude Code, log diagnostyczny). Bramki przed wydaniem 20/21 — jedyna czerwona (`test-detected-model` [11a]) **NIE jest regresją**: mierzy STAN KOMPUTERA (obecność pobranego katalogu modeli w HOME), nie kod; identyczny wynik na v1.0.28, mapa modeli nietknięta. ⚠️ Osiem z dziewięciu poprawek **wciąż nie klikniętych przez usera**.

- **1.0.28** — największe wydanie od 1.0.27 (25 commitów kodu, +9368 linii): **Chmura Faza 1** (Dysk Google, paczka szyfrowana AES-256-GCM), **katalog modeli** samoaktualizujący się + Opus 5 + pasek pokazujący model WYKRYTY, **lupa** (szukanie w całej rozmowie), **koniec trzeszczenia dźwięku** (`04d880a`), **terminal bez rozstrzelonych liter** (`1c625c8`), nadganianie lektora, wykrywanie wyścigu `/login`, automat retencji kanału. `SKIN_VERSION` bez zmian (doszedł tylko NOWY klucz `icon_search_color`, a to nie kasuje kolorów usera).
  - ⭐ **`cryptography` w paczce POTWIERDZONE POMIAREM NA WSZYSTKICH 3 SYSTEMACH** (zamyka ostrzeżenie z Fazy 1 chmury): PyInstaller ma STANDARDOWY `hook-cryptography.py` w `_pyinstaller_hooks_contrib`, więc działa bez własnego hooka wszędzie. Dowody: macOS `_rust.abi3.so` 11,8 MB w `.zip` · Windows `cryptography\hazmat\bindings\_rust.pyd` w logu pakowania Inno · Linux hook w logu builda. Paczka urosła 198→204 MB.
  - ⚠️ **Wydane ŚWIADOMIE z otwartym 🔊** („czytaj ostatnią" myli się ~17%) — to nie regresja, jest też w 1.0.27. Diagnostyka jedzie w paczce, ale u obcych userów NIEAKTYWNA (brak `CVA_READ_LAST_DEBUG=1` i brak pliku-znacznika); ma twardy limit 512 KB i pisze tylko przy kliknięciu. ⚠️ U USERA znacznik ISTNIEJE → zainstalowana 1.0.28 też będzie pisać log (ten sam `~/.vibe-coding-assistant/`) — to celowe, bo bug wciąż mierzymy.
  - **Dwa fałszywe alarmy z tego wydania (obie sondy, nie paczki):** `innoextract` NIE otwiera naszego instalatora („Could not determine setup data version") → jego `0 trafień cryptography` to artefakt, nie brak; oraz `awk '{print $NF}'` na liście `unzip` **ucina ścieżkę ze SPACJAMI** („Vibe Coding Assistant.app" → „Assistant.app") → pusty odczyt wersji. Za każdym razem ratunkiem było drugie, niezależne źródło (log budowania / poprawione cięcie).
- **1.0.27** — pierwsze wydanie z całym redesignem „Vibe Purple" + naprawy: pliki pamięci w ostatnich zakładkach (`0361565`), 🔊 BUG #6 (`0ce9609`), polskie znaki w WebTerminalu (`c38775f`), STT przez bramkę AI Managera (`eab76b3`), jeden silnik terminala wszędzie (`1dc983a`). `SKIN_VERSION` = 4. ⚠️ **Mac i Windows dostały redesign PIERWSZY RAZ tym wydaniem**, paczki na tych systemach nie były z nim uruchomione przed publikacją → przy „dziwny wygląd na Macu/Windows" zacznij od tego.
- **1.0.26:** `claude --session-id <uuid>` przypina czytnik do dokładnego pliku sesji. ⚠️ Bez `--session-id` (ręczny `--resume` po crashu) czytnik jest odpięty → 🔊 czyta złą wypowiedź.
- **1.0.25 (rebranding „Vibe Coding Assistant"):** ⚠️ infrastruktura ZOSTAJE stara (`/cva/` URL, appcast, repo `claude-voice-assistant`, wewn. binarka) — zmiana zerwałaby self-update. Rebrand MUSI obejmować też `.ps1/.yml/.command` (pominięcie = padł build Windows).
- **Wieloplatformowość:** Linux (kod + AppImage), macOS (.dmg/.zip), Windows (.exe Inno). PL/EN (domyślnie EN; PL gdy system polski); en-GB usunięty w 1.0.17 (scalony do en-US + migracja wsteczna w `set_ui_language`).
- **Self-update:** macOS ✅ (1.0.8) · Windows ✅ (1.0.14) · Linux ✅ (kod 1.0.16, feed 1.0.17). Pipeline WYDANIA potwierdzony e2e 2× (1.0.18, 1.0.19); uruchomienie spakowanego AppImage potwierdzone 2026-06-20. **KLIENCKI cykl self-update (feed→pobierz→podmień→wstań) POTWIERDZONY NA MACU 2026-08-07** (1.0.27→1.0.28) — zostają Windows i Linux. Podejrzenie crashu Mac/Win z braku wtyczek Qt ODRZUCONE (PyInstaller dociąga `cocoa`/`qwindows` sam).
- **Kanał:** `https://pobierz.srv1251441.hstgr.cloud` (strona za basicauth) + publiczny feed `…/cva/appcast.json`. Kontener `cva-web` (nginx) na VPS w `/opt/cva-web/`.

**KONWENCJA KOLORU (ustalona z userem): skórka rządzi SPOCZYNKIEM, kod niesie STAN.** Kolory znaczeniowe (`DANGER` nagrywanie/stop, `SUCCESS` czytanie/wznów) siedzą w kodzie — w skórce dałoby się ustawić zielone nagrywanie i sygnał przestałby znaczyć. Nie dokładaj kluczy skórki „per stan" (39 kluczy = dialog i config bez zmian). „Biel" = `theme.TEXT` `#eae6f2`, NIE `#ffffff` (czysta biel tylko na Wyślij, bo leży na gradiencie).
- **„Gdzie jestem" to też STAN:** `theme.TAB_ACTIVE` NIE idzie ze skórki (choć kusi) — inaczej dałoby się ustawić aktywną zakładkę CIEMNIEJSZĄ od reszty.
- ⚠️ **Odcień niosący SYGNAŁ musi mieć MARGINES kontrastu, nie tylko „inny numerek".** Redesign zszedł z 15% do 4% wobec tła i wygasił dwa sygnały naraz, choć kod działał. Mierz różnicę wobec SĄSIADA (średnia RGB), próg roboczy **≥20/255 (~8%)** — nie oceniaj na oko w edytorze.
- **Redesign „Vibe Purple"** (`bc81a03`+`410c9ab`): `src/gui/theme.py` = jedyne źródło kolorów; **zmiana palety wymaga podbicia `SKIN_VERSION`**, inaczej jest NIEWIDOCZNA (config nadpisuje). → `redesign-vibe-purple.md`, `qt-pulapki-qss-redesign.md`
  - ⭐ **ALE: DODANIE nowego klucza skórki podbicia NIE wymaga** — i nie wolno go robić bez potrzeby, bo skasowałoby kolory usera. `_load_settings` iteruje po `DEFAULT_SKIN_COLORS` i bierze z `config.json` tylko klucze, KTÓRE TAM SĄ; nowego nie ma, więc zostaje wartość domyślna. Podbicie jest konieczne wyłącznie przy ZMIANIE wartości istniejącego klucza. (Sprawdzone przy `icon_search_color`, 2026-08-04.)

## KOMERCJALIZACJA 2026 (branding + strona)

Plan z `docs/PRD.md` ruszył od brandingu/strony, NIE od kodu monetyzacji.
- **Nazwa publiczna „Vibe Coding Assistant" (VCA)**, wewn. kod zostaje `claude-voice-assistant` (CVA). Marka bez „Claude". Domena DO KUPIENIA (wolne `.app`: preferowana `vibe-code-app`). → `vca-nazwa-domena.md`
- **Logo** („Prompt Wave": `>` + fala): `branding/logo/` (SVG, PNG 16–1024, `vca.ico`, `vca.icns`, favicon; `rsvg-convert` + Pillow). Wpięte w apkę → `src/assets/icon.{png,svg,128,64}`; paczki generują `.icns`/`.ico` z `icon.png`. Galeria: `branding/logo-concepts.html`.
- **Strona produktowa (staging, NA BRUDNO):** `website/` → VPS `/opt/cva-web/html/cva/staging/` (publiczny, `noindex`). PL+EN: loader, hero, funkcje, cennik Free/Pro, pobieranie, FAQ, kontakt. **Makiety** (alert): płatności, pobieranie, formularz. Przełącznik 🌙/☀️ + auto-język po IP (GeoJS; ręczny wybór w `localStorage` nadrzędny).
- **Dokumenty prawne (robocze):** polityka/licencja/regulamin/cookies PL+EN w `website/`. Dostawca = **Fulfillment Polska**; placeholdery `[forma prawna/adres/NIP]`. ⚠️ przed publikacją: przegląd prawny + DOPISAĆ geolokalizację (GeoJS wysyła IP). → `sekcja-prawna-stan.md`
- **Funkcje po kolei:** #1 ✅ ikona per zakładka · #2 ✅ kolor zakładki + ramka okna aktywnego agenta (1.0.21) · #3 ✅ głos per agent (1.0.21; **322 darmowe głosy edge-tts**: 2 PL, 47 EN, z `edge_tts.list_voices()`).
- **Panel admina:** backend Fazy A gotowy w `server/` (FastAPI+Postgres: licencje/pobrania/wersje); frontend z cloud.co.design pending; Faza B = płatności/deploy. → `panel-admin-stan.md`

## MODELE — katalog samoaktualizujący się (2026-07-26)

**Alias = ZAWSZE NAJNOWSZY model rodziny.** `claude --model opus` to dziś **Opus 5**, `sonnet` → **Sonnet 5** (zweryfikowane na żywo; pomoc CLI mówi wprost „alias for the latest model"). Dlatego etykiety z numerem wersji starzeją się PO CICHU — apka pokazywała „Opus 4.8", uruchamiając Opus 5, a licznik tokenów dawał Sonnetowi 200 tys. zamiast 1 mln.

**Nazwa modelu ma JEDNO źródło:** `config.CLAUDE_MODELS` (+ nadpisanie z sieci). Tłumaczenia niosą wyłącznie OPIS (`model_*_desc`); etykieta = nazwa + opis (`model_label()`). ⚠️ Wcześniej nazwa żyła w 6 miejscach (2 słowniki + PL + EN) — stąd rozjazd. Nie przywracaj `model_*_full`/`_short`.

**`core/model_catalog.py`** pobiera nazwy i okna kontekstu z `platform.claude.com/docs/en/about-claude/models/overview.md` (czysty `.md`, bez klucza API i bez logowania), cache w `~/.vibe-coding-assistant/models-cache.json`.
- ⛔ **FAIL-OPEN**: brak sieci / zmieniony układ strony / uszkodzony plik → wartości wbudowane, apka działa jak wcześniej. Parser ma bramkę przytomności (<2 modele z oknem kontekstu = błąd, nie cicha podmiana dobrych danych śmieciami).
- **Sieci NIE MA w imporcie `config.py`** — pobiera wątek tła 5 s po starcie i tylko gdy cache >7 dni; ręcznie: Ustawienia → „Sprawdź nowe modele".
- ⚠️ **Nowej RODZINY nie dodajemy sami** — nie wiadomo, czy `claude --model <alias>` przyjmie ją w CLI usera; apka o niej INFORMUJE (`new_families`), decyduje człowiek.
- ⚠️ **Nałożenie katalogu MUTUJE słowniki w miejscu** (`apply_model_catalog`) — inne moduły zrobiły `from config import CLAUDE_MODELS` i trzymają referencję; podmiana przypisania byłaby dla nich niewidoczna.
- Bramka: `tools/test-model-catalog.py` (61/0) — parser na PRAWDZIWEJ stronie w `tools/fixtures/`, z kontrolami negatywnymi.

**Czego uruchomi „Domyślny", apka NIE WIE z góry** — dowiaduje się po fakcie z dziennika (`message.model`); mapę `identyfikator API → klucz` trzyma `config.CLAUDE_MODEL_API_IDS` (katalog + wpisy wbudowane). Stąd pasek „Domyślny (Opus 5)" — patrz „⏳ CZEKA NA TEST NA ŻYWO".

**Dodanie modelu ręcznie** (gdy trzeba przypiąć konkretną wersję): klucz = to, co idzie do `claude --model` (pełna nazwa, np. `claude-opus-4-8`, nie alias — alias by się przesunął). Dopisz do `CLAUDE_MODELS` + `CLAUDE_MODEL_CONTEXT_LIMITS` + opcjonalnie `model_<klucz>_desc` w OBU językach. Zweryfikuj na żywo: `claude --model <klucz> -p "OK"` → exit 0 (⚠️ przez `env -u CLAUDECODE …`, patrz COMMON).
**Zużycie — ZMIERZONE na 42 tys. wypowiedzi z dzienników usera (2026-07-26), nie z cennika:** Opus 4.8 = 100%, **Opus 5 = 118%**, **Fable 5 = 177%** (Fable ma 2× cenę za token — to on zjada limit, nie Opus 5). Cena za token Opus 5 = identyczna jak 4.8; różnica siedzi w większym przeładowywanym kontekście, a nie w dłuższych odpowiedziach (wyjście na turę jest wręcz o 6% KRÓTSZE). ⚠️ Część z tych 18% to dłuższe sesje, nie model (rozrzut per projekt 0–36%). ⛔ **Nie powtarzaj mojego błędu:** napisałem userowi, że „Opus 5 domyślnie myśli, więc zużyje więcej" — to prawda dla SUROWEGO API, ale Claude Code ustawia `thinking` sam, więc go nie dotyczy; pomiar to obalił. Metoda pomiaru → COMMON „Monitorowanie ZUŻYCIA TOKENÓW Claude Code".
⚠️ **`~/.claude.json` ma wpisane `model: claude-opus-4-8`, a bez `--model` odpowiada Opus 5** (zmierzone w dwóch katalogach). Ten wpis to martwa pozostałość, której CLI już nie honoruje — **nie czytaj z niego domyślnego modelu**, zmierz `claude -p … --output-format json` i sprawdź `modelUsage`. Stąd etykieta „Domyślny (z konfiguracji Claude Code)" celowo BEZ numeru wersji.

## ⚠️ Pamięć/RAM — każda zakładka = osobny proces `claude`

Apka w Pythonie jest lekka (~80 MB). Pamięć zżera **Claude Code CLI: 3–5 GB NA ZAKŁADKĘ** (node, rośnie z długością sesji i kontekstem startowym). 4 auto-startujące zakładki × ~4 GB przebijają RAM → swap → zawieszanie (i spowolnienie dyktowania). Łagodzenie: mniej auto-startu, restart długich zakładek (Stop→Uruchom / `/clear`), szczuplejsze pliki pamięci, `earlyoom` jako siatka. Sam dokup RAM nie wystarczy. → `pamiec-ram-claude-cli.md`, `run-safe-limity-ram.md`

## 🟡 „Please run /login" = WYŚCIG O ODŚWIEŻENIE TOKENU — ETAP 1 (obserwacja) ZROBIONY (`e6af2c3`)

**User NIE był wylogowany** — to zakładki VCA przewracają się nawzajem. Zmierzone 2026-07-20: 8 procesów `claude` na jednym `~/.claude/.credentials.json`, 5 odmów w 4 zakładkach w 5 minut wokół wygaśnięcia (16:51), plik odnowiony 16:55. Proces NIE ginie — wisi z martwą sesją, więc wygląda na zawieszenie. ⚠️ To NIE wina kolektora AI Managera (on plik tylko CZYTA).

**Stan:** apka WYKRYWA odmowę, po opóźnieniu orzeka „wyścig vs prawdziwe wylogowanie", zapisuje do `login-events.log` + komunikat na pasku. Automatycznego restartu NIE MA — świadomie, do potwierdzenia werdyktów na żywych danych.
1. ✅ **Wykryj:** `TranscriptReader._is_api_error` + `take_api_errors()` → `_on_claude_api_error`. ⚠️ Rozpoznanie po **PIECZĄTCE `isApiErrorMessage`**, NIGDY po treści — fraza „Please run /login" występuje w normalnej rozmowie (choćby w tych plikach pamięci) → dopasowanie tekstowe restartowałoby zakładkę, gdy ktoś o usterce *napisze*. Kontrola negatywna w `tools/test-login-race.py`. Przy okazji błędy przestały iść do lektora.
2. ✅ **Odróżnij wyścig od PRAWDZIWEGO wylogowania** (bez tego pętla restartów): `platform_utils.claude_credentials_state()` + `credentials_refreshed_since()`. ⚠️ **Werdykt MUSI zapadać Z OPÓŹNIENIEM** — zwycięzca odnawia plik po kilku minutach (zmierzone: 8 min) → ocena natychmiastowa orzekłaby „wylogowanie" dla KAŻDEGO wyścigu; dopytujemy co minutę przez 12 min. ⚠️ Na macOS poświadczenia są w Pęku kluczy, nie w pliku → `available=False` = werdykt „nierozstrzygnięty" + rada „wpisz /login".

⭐ **PIERWSZE PRAWDZIWE 401 „token has expired" — Mac, 2026-08-07, 1.0.28 (zrzut usera).** Ekran: `Please run /login · API Error: 401 OAuth access token has expired`, a NA PASKU nasz komunikat „Zakładka apka: błąd logowania — sprawdzam, czy to wyścig zakładek…" = **mechanizm z `e6af2c3` żyje i odpalił się na realnym zdarzeniu** (wcześniej wywołał go tylko błąd SIECI `ENOTIMP`). ⚠️ To **NIE był wyścig, tylko autentyczne wygaśnięcie**: JEDNA otwarta zakładka (wyścig wymaga kilku) + Mac po długim uśpieniu (`Time Awake Since Boot` ≈ 12,7 dnia). Werdykt na macOS z definicji wyjdzie „nierozstrzygnięty" (poświadczenia w Pęku kluczy, nie w pliku) + rada „wpisz /login" — czyli zachowanie ZGODNE z projektem, nie usterka. Lek dla usera: `/login` w polu zakładki — ✅ **potwierdzone u usera 2026-08-07: `/login` odblokował zakładkę**, co domyka diagnozę „prawdziwe wygaśnięcie, nie wyścig" (gdyby to był wyścig, `/login` nie byłby potrzebny — plik odnawia zwycięzca). ⚠️ Do domknięcia Etapu 1 wciąż brakuje werdyktu z LINUKSA (tam plik poświadczeń jest czytelny i werdykt bywa rozstrzygający).
- ✅ **POTWIERDZONE NA ŻYWO przy okazji: `Auto-update failed: no write permission to npm prefix`** (Claude Code v2.1.183 z npm na Macu) — osobna, NIE nasza usterka; leczy ją natywny instalator zamiast npm. Występuje RAZEM z 401 i łatwo je pomylić: to dwie różne rzeczy w dwóch czerwonych liniach.

**ETAP 2 (auto-restart) — włączyć DOPIERO gdy `login-events.log` pokaże bezbłędne werdykty na żywych danych:** (3) restart zakładki z TYM SAMYM `--session-id` (`main_window.py` ~2344; `TranscriptReader.pin_session`) zachowuje wątek rozmowy; (4) bezpieczniki: 1 restart na zdarzenie + cooldown ~2 min per zakładka + licznik prób, po drugiej nieudanej — powiedz userowi wprost; restart NIE może wejść w środek pisania (`waiting_for_user()`); (5) testy OBA kierunki: wyścig → jeden restart, prawdziwe wylogowanie → zero restartów i komunikat.
⛔ **Nie dokładaj w VCA własnego odnawiania tokenu** — to DZIEWIĄTY uczestnik wyścigu. **Łagodzenie bez kodu:** mniej jednoczesnych zakładek (zbieżne z notatką o RAM).

---

## CHMURA — Faza 1 KOMPLETNA W KODZIE (2026-07-21)

Plan: `docs/PLAN-CHMURA-SYNC.md` (sekcja 9 = szyfrowanie). Pamięć: `chmura-sync-agentow.md`.
- **Silnik:** `src/core/cloud/agent_bundle.py` + `bundle_crypto.py` (AES-256-GCM + scrypt), testy 26/26 + `tools/test-cloud-bundle.py`. **Paczka niesie:** agentów (ikony/kolory/głosy), pliki pamięci, definicje skilli, gating MCP, szybkie akcje, projekty pamięci, skórkę, język **oraz klucze API** (decyzja usera). NIE niesie kodu projektów (idzie przez `git clone`) ani `claude_command`.
- **Dysk Google działa na żywo** (`a7663f5`+`a6c5ddc`): `google_drive.py` — OAuth desktop + PKCE, loopback `127.0.0.1`, zakres `drive.file` (apka widzi TYLKO własne pliki → bez audytu Google), token 600. Projekt usera: `impressive-bay-503111-d1` („VCA Google"), klient `installed`; dane w `~/.vibe-coding-assistant/cloud-google-{client,token}.json`. ⚠️ Aplikacja MUSI być w trybie **Produkcja** (nie „Testowanie") — inaczej Google kasuje bilet odnowienia po 7 dniach.
- **Ekran „Chmura"** (`src/gui/cloud_dialog.py`, menu Ustawienia; prosty — user odrzucił makietę Cloud Design): konto, hasło paczki, wyślij/pobierz. Sieć w wątku roboczym (logowanie czeka minuty na zgodę w przeglądarce). Testy 22. **Hasło:** apka proponuje kod z `generate_passphrase()`, user może wpisać własne; zapamiętane w `cloud-passphrase.txt` (600). Funkcja docelowo **Pro** (`license_manager` to zaślepka — miejsce na sprawdzenie licencji oznaczyć, ale NIE udawać blokady).
- **Przeprowadzka potwierdzona na prawdziwych danych i prawdziwym Dysku:** 10 agentów, 1098 KB, 12 plików pamięci, 273 pliki skilli, **8 projektów do `git clone`**, 0 ostrzeżeń; klucze API dojechały, `claude_command` NIE. ⚠️ **Pliki pamięci z repo gitowych są ŚWIADOMIE pomijane przy imporcie** (przyjdą z klonem; kopiowanie wywaliłoby klon do niepustego katalogu) → okno MUSI mówić, ile projektów czeka na sklonowanie (`7daddb4`), inaczej user widzi agentów bez kodu i zgłasza to jako błąd.
- ⚠️ **Etykieta bez jawnego `color:` = czarny tekst na czarnym tle** (`24eb386`) — Qt bierze wtedy barwę z palety. W `cloud_dialog` pilnuje tego reguła strukturalna w teście (każdy `QLabel` ma `color:`) ze sprawdzoną kontrolą negatywną. ⚠️ Podgląd renderowany z WŁASNYM, doraźnym QSS tego NIE pokazał.
- ⚠️ **Znane ograniczenie: KASOWANIE NIE PROPAGUJE SIĘ** (import tylko dodaje i nadpisuje) — do rozwiązania przy auto-synchronizacji (Faza 2/3).
- ⚠️ **`cryptography` + PyInstaller** działa BEZ własnego hooka (`_rust.abi3.so` wchodzi samo) — zweryfikowane TYLKO na Linuksie; Mac/Windows do potwierdzenia przy najbliższym buildzie CI.

---

## Inne otwarte TODO

- 🧹 **Dług: sprzątnąć diagnostykę 🔊** — skasować znacznik `~/.vibe-coding-assistant/read-last-debug.on` i usunąć czujnik z kodu. Bug 🔊 zamknięty 2026-08-26 (runda 6), diagnostykę trzymaliśmy jedną rundę na wypadek nawrotu. ⚠️ **ZMIERZONE 2026-09-17: znacznik nadal istnieje** (0 B, z 3.08), a `read-last-debug.log` urósł do **432 kB** — poprzednia taka diagnostyka doszła do 99 MB, więc to nie jest kosmetyka. ⚠️ Przy sprzątaniu **NIE usuwaj `_GATED_LOG_NAMES`** (`9e5bedc`) — to on chroni log przed kasowaniem przy starcie; bez niego następna runda diagnozy znów straci dowody. (Pozycja wróciła tu 2026-09-17 przy konsolidacji: siedziała w sekcji historycznej i wyjechała z nią do archiwum — złapała to bramka `sprawdz-konsolidacje.py`.)
- ~~**XSS w `web_terminal._show_failure_page`**~~ ✅ **BYŁO ZROBIONE JUŻ WCZEŚNIEJ** (`6d36a5a`) — `reason` i ścieżka logu idą przez `html.escape`, przy kodzie stoi komentarz z powodem. TODO wisiało nieaktualne i przy sprawdzaniu listy 2026-08-26 kosztowało jedno wywołanie. ⛔ Trwała nauka: **pierwszym krokiem zadania z listy jest POMIAR, nie plan** — to już drugi taki przypadek w tym pliku (poprzedni: „sprzątanie starych plików" kłamało miesiąc).
- ~~**Wykrywanie ZEPSUTEGO `claude`**~~ ✅ ZROBIONE `7d0a7bc` (2026-08-12) — patrz `CLAUDE-VOICE-ASSISTANT-PULAPKI.md` („TRWAŁE PUŁAPKI PROJEKTU"); zostaje test na żywo.
- ~~**Sprzątanie po aktualizacji**~~ ✅ ZROBIONE (automat 2026-07-13, domknięte `9e5bedc` 2026-08-18). ⛔ **Trwała nauka, bo TODO KŁAMAŁO przez miesiąc:** automat istniał i działał (`updates/` trzymał 1 paczkę), a notatka dalej straszyła „zbiera GB" — pierwszym krokiem był POMIAR, nie plan naprawy. Przy okazji wyszła realna usterka: `read-last-debug.log` stał na liście martwych logów (`_STALE_LOG_NAMES`) — słusznie, gdy czujnik był skasowany (`9ae59c3`), ale przy rundzie 5 czujnik ODŻYŁ i **każdy start apki kasował dziennik dowodowy trwającej diagnozy 🔊**. Teraz `_GATED_LOG_NAMES`: log ginie TYLKO przy wyłączonym czujniku (brak znacznika **i** brak zmiennej). ⚠️ Nie przenoś tych nazw z powrotem „bo są martwe" — najpierw `grep` po nazwie w `src/`. Dołożone też: zrzuty crashu starsze niż 30 dni (podłoga 5 — inaczej po długim spokoju ginie CAŁA historia; obejmuje `terminal-glitch-*.log`, których limit SZTUK nie widział) oraz limit 512 KB na `tts.log`. Bramka `tools/test-cleanup.py` 23/23, sabotaż 7/7. → `todo-sprzatanie-starych-plikow.md`
- [ ] 🔴 **BUG Mac — apka pada przy ZAMYKANIU (kolejność niszczenia PyQt). ⛔ KOREKTA: to NIE jest crash QtWebEngine ani skutek snu** — poprzednia teza żyła 6 tygodni i była WPROST NIEPRAWDZIWA. Wystąpienia: 1.0.25 · 1.0.26 · 1.0.28 (2026-08-07). **Pełny stos (`Documents/Translated Report Full.txt`) czytany od dołu to sekwencja WYJŚCIA:** `_Py_HandleSystemExit` → `tb_dealloc`/`frame_dealloc` → `dealloc_QApplication` → `~sipQMainWindow` → `hide_sys()` → `QApplication::setActiveWindow()` → zdarzenie do `sipQTabWidget::event` → `QWidget::palette()` → SEGFAULT pod adresem spoza obszarów VM.
  - **Przyczyna:** `src/main.py:124` `sys.exit(app.exec_())`, gdzie `app` i `window` to LOKALNE zmienne tej samej funkcji (`main.py:120`). `SystemExit` trzyma traceback → ramkę → OBA obiekty, więc giną naraz i w niekontrolowanej kolejności: `QApplication` PIERWSZY, a `MainWindow` już w środku jego destruktora → Qt rozsyła zdarzenia do zwolnionych widżetów.
  - ⚠️ **`Crashed Thread: 0 CrBrowserMain` NIE ZNACZY „padło w QtWebEngine"** — Chromium tylko PRZEMIANOWUJE wątek główny. Kontrola: ramek `webengine|chromium|blink` w stosie wątku 0 = **0**.
  - ⚠️ **`Time Since Wake` był fałszywym tropem** (88 s / 142 s). W stosie nie ma nic o wybudzeniu; najprostsze wyjaśnienie korelacji: user wraca po śnie i ZAMYKA apkę. **Do potwierdzenia u usera: czy apka znika sama, czy dopiero przy zamykaniu.**
  - ⚠️ **Ta sama pułapka jest na KAŻDEJ platformie** (kod wspólny) — na Linuksie po prostu jeszcze nie strzeliła. Praca usera nie ginie (okno zdążyło się zamknąć), widoczny jest tylko raport Apple.
  - ✅ **NAPRAWIONE W KODZIE** (`9e3afde`): `rc = app.exec_()` → `del window` → `gc.collect()` → `sys.exit(rc)` + komentarz-ostrzeżenie w `main.py` („nie upraszczaj z powrotem do jednej linii"). Bramka `tools/test-exit-order.py` mierzy KOLEJNOŚĆ niszczenia w podprocesach: stary wzorzec → `APP, WINDOW` (zła), nowy → `WINDOW, APP` (dobra); kontrola negatywna WBUDOWANA (gdyby stary wypadał dobrze, test sam ogłasza się bezwartościowym).
  - ⛔ **POPRAWKI NIE MA W ŻADNEJ WYDANEJ PACZCE — decyzja usera 2026-08-07: pojedzie z NASTĘPNYM wydaniem** (1.0.28 jej NIE zawiera, bo powstała po niej). Nie szukaj jej w paczkach i nie zakładaj, że user ją testuje.
  - ⚠️ **Bramka mierzy PRZYCZYNĘ, nie skutek** — na Linuksie zła kolejność uchodzi płazem, więc zniknięcie raportu Apple potwierdzi WYŁĄCZNIE Mac usera po następnym wydaniu.
  - ❓ **PYTANIE BEZ ODPOWIEDZI (zadane 2×, nie naciskać):** czy apka na Macu znikała SAMA, czy okienko Apple pojawiało się dopiero przy jej zamykaniu? Jeśli sama — poprawka usuwa tylko brzydkie okienko przy wyjściu, a przyczyna samoczynnego zamykania zostaje. → `mac-qtwebengine-crash-po-snie.md`
- [ ] **Mac — Claude Code przez npm: 401 + „Auto-update failed: no write permission to npm prefix"** (NIE bug naszej apki; macowy odpowiednik problemu z Windows). `/login` odblokowuje agenta; auto-update naprawić osobno (natywny instalator zamiast npm).
- [ ] **Redesign — do rozważenia:** własna belka tytułowa (świadomie pominięta — ryzyko Wayland/macOS); okno ustawień jako jeden modal z bocznym menu (to zmiana UKŁADU, poza zakresem redesignu). Makieta żyje w repo: `design/makieta-2026-07-09/` (`settingsTabs` = niewdrożony ekran); zrzuty POMINIĘTE, bo repo jest publiczne (pełny eksport: `~/Projekty/makiety/vca-redesign-2026-07-09.zip`). ⚠️ `.gitignore` ma `*.zip` — kolejny eksport rozpakuj do `design/`, nie commituj archiwum.
- [ ] **Strona/branding (decyzje usera):** dane firmy `[forma prawna/adres/NIP]` w polityce+licencji (PL+EN), zakup domeny, geolokalizacja GeoJS w polityce, link „Instrukcje" (teraz `#`) → `instrukcja-instalacja.html`, przegląd prawny, przepięcie ze stagingu na docelową domenę.
- [ ] Test zadania „nodecli" instalatora na świeżym Windows (auto-instalacja Node+Claude Code).
- [ ] Kolejne języki: słownik w `UI_TRANSLATIONS` (parytet!) + `SUPPORTED_LANGUAGES` + `detect_system_language` + pliki `-xx.html` + dropdown.
- [ ] Potwierdzić kliencki self-update spakowanego AppImage.
- [ ] ⚠️ **Lupa jest jedynym przyciskiem paska POMINIĘTYM w `_apply_skin_icons`** (`main_window.py`, pętla po `dictate/read/pause/stop/copy/clear_input/add_media/quick_actions/repair_terminal`) — więc zmiana „Kolor ikony…" w oknie Skórka jej NIE DOTYCZY i lupa zostaje przy poprzedniej barwie. Znalezione 2026-09-15 przy przenoszeniu sygnału do ikony, ŚWIADOMIE nienaprawione (poza zakresem zgłoszenia). To czwarte wystąpienie rodziny „nowy przycisk trzeba dopisać w OBU miejscach". Objaw zerowy, dopóki user nie zmieni koloru ikon. Poprawka to jedna linia w pętli + asercja w `tools/test-bottom-bar-icons.py`.
- [ ] 🔴 **Program SAM wpisuje sobie martwy skrót npm do `config.json`** (zmierzone u współpracownika 2026-09-03: `C:\Users\dell\AppData\Roaming\npm\claude.CMD --session-id …` → `System nie może odnaleźć określonej ścieżki` przy KAŻDYM starcie). `claude_binary_health()` czyta pierwsze bajty CELU nakładki `.cmd`; gdy cel **nie istnieje**, nie orzeka „broken" i fail-open przepuszcza martwy skrót — `find_claude_command()` bierze go jako pierwszy. **Trzeci stan, którego nie znała pamięć: nie atrapa (zła treść), tylko szyld po nieudanej instalacji (brak celu).** Lek: traktować nierozwiązywalny cel jak `broken` i przechodzić dalej po liście kandydatów.
- [ ] 🔴 **Kreator „Program setup — what's still left" NIE uprzedza, że Claude Code wymaga PŁATNEGO planu.** Wymienia wyłącznie kroki techniczne (zainstalowane / zalogowane / klucz Groq), więc user przechodzi całą instalację i logowanie, żeby dowiedzieć się na końcu, że darmowe konto nie wystarcza (zmierzone 2026-09-03: ~godzina straconej pracy). Dopisać pozycję o planie **na początku** listy.
- [ ] ⚠️ **Nasza strona instalacji Windows uczy złej drogi:** `packaging/web/instrukcja-instalacja.html` każe `npm install -g @anthropic-ai/claude-code`, a **zero wzmianki** o blokadzie skryptów PowerShella i o instalatorze natywnym (`grep`: 0 trafień `install.ps1`, 0 `ExecutionPolicy`). To ona wprowadziła współpracownika w ścianę. Przestawić instalator natywny na pierwsze miejsce, npm zostawić jako wariant zapasowy.
- [ ] ⚠️ **Język: na POLSKIM Windowsie okna wyszły po ANGIELSKU** (zrzut 2026-09-03) — `detect_system_language` na Windows zachowuje się inaczej, niż zakładaliśmy. Zmierzyć, zanim ruszysz kod.

## Częste problemy

| Problem | Rozwiązanie |
|---------|-------------|
| QTermWidget not found | `pip install wheels/qtermwidget-*.whl` |
| TTS nie działa | internet (edge-tts); `~/.vibe-coding-assistant/tts.log` |
| STT nie nagrywa | `GROQ_API_KEY`, mikrofon |
| `claude` not recognized | Node + `npm i -g @anthropic-ai/claude-code` + restart |
| `claude.exe` „aplikacja 16-bitowa" (Windows) | ATRAPA po niewykonanym `postinstall` npm — 6 kroków w `windows-claude-niezgodny-npm.md`; skrót: `npm uninstall -g` + `irm https://claude.ai/install.ps1\|iex` + w apce komenda = `claude` |
| Apka nie startuje | `python3 -m py_compile src/main.py` |
| Zawieszanie przy 2+ zakładkach | RAM — patrz sekcja „Pamięć/RAM" w tym pliku |
| Kopiowanie/odczyt zaznaczenia zwraca pustkę | Claude (TUI) przejmuje mysz → zaznaczaj z **Shift** (nie błąd kodu) |

## PODŁĄCZENIE DO AI MANAGERA — ✅ działa, ⏳ JEDNA RZECZ OTWARTA (przepięcie na zadania)

Rozmowa z Claude idzie przez CLI (nie HTTP) → bramka jej nie łapie i nie musi; zużycie liczy osobno kolektor Claude Code AI Managera z lokalnych `.jsonl`.
**STT (dyktowanie) na bramce** (potwierdzone 2026-07-13): `POST https://ai.srv1251441.hstgr.cloud/v1/audio/transcriptions`, `Authorization: Bearer aim-…` (klucz aplikacji **„VCA" (id=3)**), model z prefiksem `groq/…`; język auto = NIE wysyłaj pola `language`; kody: `401` zły klucz · `429` limit · `503` brak wolnego konta. → `stt-bramka-ai-manager.md`
⚠️ NIE mylić z osobną apką „Voice Assistant" (repo `voice-assistant`) — inny projekt, inny klucz.
⭐ **Obie potrafią chodzić JEDNOCZEŚNIE i na liście procesów wyglądają identycznie** (`python3 src/main.py`). Rozstrzyga KATALOG ROBOCZY, nie nazwa: `readlink /proc/<pid>/cwd` — VCA ma `claude-voice-assistant`, tamta `Voice Assistant` (ze spacją). Przy diagnozie sprawdź też `tr "\0" "\n" < /proc/<pid>/environ | grep CVA_`, żeby wiedzieć, KTÓRY silnik terminala działa. (2026-09-11: wziąłem cudzy proces za betę VCA.)

✅ **ZROBIONE 2026-09-12 W KODZIE — dyktowanie woła ZADANIE `task/transcribe`** (`9b5729b`).
Wcześniej wołaliśmy model PO NAZWIE, więc bramka rotowała tylko między dwoma kontami TEGO SAMEGO
dostawcy — awaria Groqa = dyktowanie martwe bez zejścia. Zmiana to była jedna linia; `STT_API_URL`,
brak pola `language`, klucz id=3 i zakaz własnego łańcucha zapasowego zostały BEZ ZMIAN (§4 kontraktu).
📄 Kontrakt (ich źródło): `~/Projekty/AI Manager/docs/KONTRAKT-VCA-ZADANIA.md` ·
📄 nasza zwrotka (nasze źródło): `docs/ZWROTKA-AI-MANAGER-TRANSCRIBE.md`

⛔ **STAN JEST POŚREDNI — nie czytaj tego jako „działa u usera":**
| gdzie | co woła |
|---|---|
| kod w repo | ✅ `task/transcribe` |
| **beta na której pracuje właściciel** | ❌ stary proces, konfiguracja wczytana przy starcie — **do restartu nic się nie zmienia** |
| **wydana paczka 1.0.29** | ❌ nadal `groq/whisper-large-v3` |

✅ **ŁAŃCUCH PRZESTAWIONY — RESTART ODMROŻONY (2026-09-12, po południu).** AI Manager przestawił
`transcribe` na produkcji: na czele stoi teraz **pełny `whisper-large-v3`**, turbo zeszło na koniec,
krok środkowy (cloudflare) bez zmian. **Potwierdzone NASZYM pomiarem, nie ich deklaracją** — ten sam
plik kontrolny co rano: `x-aim-model: groq/whisper-large-v3` (rano było `…-turbo`), `200` w 1,47 s.
**Czyli restart bety jest dziś CZYSTYM ZYSKIEM: lepszy model NIŻ przed przepięciem + siatka u drugiego
dostawcy.** ⚠️ Zmiana poszła u NICH z panelu — u nas nie ma czego zmieniać ani cofać.
⛔ **Historia tej decyzji, warta zapamiętania jako wzorzec:** nasze pierwsze brzmienie („2,2 punktu
na jednym nagraniu") ZANIŻAŁO sprawę; ich pomiar produkcyjny pokazał, że VCA chodziło na PEŁNYM
modelu (363 wywołania / 30 dni), więc przepięcie bez przestawienia byłoby REGRESJĄ. Wstrzymanie
restartu do czasu decyzji kosztowało pół dnia i nie kosztowało ani jednej gorszej transkrypcji.
✅ **RYZYKO PO ICH STRONIE ZDJĘTE tego samego dnia (2026-09-12 wieczorem).** Uprzedzili sami, że
przestawienie żyje w ich BAZIE, a domyślny łańcuch w ich KODZIE ma starą kolejność — czyli odtworzenie
bazy od zera przywróciłoby turbo po cichu. Domknęli to i zmierzyli: działający PROCES niesie nową
kolejność, skrypt startowy NIE nadpisuje łańcucha, wywołanie kontrolne po przebudowie oddało
`x-aim-model: groq/whisper-large-v3`, porównywacz kod↔baza `✅ ZGODNE`.
⚠️ Zapis zostaje, bo **to jest wzorzec diagnostyczny na przyszłość**: gdyby dyktowanie kiedyś samo
wróciło na turbo bez żadnej zmiany u nas, rozjazd „kod vs baza" u dostawcy jest pierwszym miejscem
do sprawdzenia. ⭐ Nauka o współpracy: oni zgłosili tę lukę SAMI, zamiast zameldować „zrobione" —
dzięki temu wiedzieliśmy, czego szukać, zanim cokolwiek padło.
⭐ Prośba o zadanie dobrane pod WIERNOŚĆ dla `STT_FIX_MODEL` — **ich właściciel odmówił świadomie**,
nasz powód zapisali jako decyzję, nie dług. Wracamy do tematu, gdy parafrazowanie zacznie szkodzić.

⭐ **DOWÓD ZE SKUTKU (nie z kodu), zmierzony na żywej bramce — nie powtarzaj tej sondy:**
`model=task/transcribe` → `200`, `x-aim-task: transcribe`, `x-aim-model: groq/whisper-large-v3-turbo`
⚠️ **(to pomiar PORANNY, sprzed przestawienia łańcucha — dziś na czele stoi PEŁNY model, patrz wyżej;
zostawiony, bo dowodzi, że zadanie działa, a nie tego, KTO dziś słucha)**,
treść kompletna · kontrola przeciwna `task/nie-istnieje` → **`400`** z listą zadań i zerem nagłówków
`x-aim-*` (§5.4 kontraktu ZAMKNIĘTY: nie ma cichej podmiany). AI Manager potwierdził ten strzał
u siebie co do znaku. Nagranie kontrolne robi się w 10 s przez edge-tts — nie trzeba prosić usera o dyktowanie.

⛔ **ASERCJA „wysłany model == stała z konfiguracji" NIE CHRONI PRZED COFNIĘCIEM — potrzebne są DWIE.**
Zmierzone sabotażem (`tools/sabotaz-dictation-fix.py S12`): cofnięcie przepięcia zapaliło **tylko F7**
(„ma zaczynać się od `task/`"), a **F6 przeszło**, bo porównuje stałą samą ze sobą — przy cofnięciu
konfiguracji obie strony porównania przesuwają się razem. F6 dowodzi wyłącznie, że wartość DOJEŻDŻA
na sieć. **Nie kasuj żadnej jako duplikatu.** Wynik: 1 padło / **50 wykonanych** (tyle samo co na
zdrowym kodzie, czyli bramka nie urwała się w połowie). Bramka: `tools/test-dictation.py` 50/50.

⚠️ **Druga nasza droga na bramkę NADAL woła po nazwie modelu i tak MA ZOSTAĆ:**
`STT_FIX_MODEL = "gemini/gemini-3.5-flash-lite"` (poprawianie transkrypcji, `/v1/chat/completions`).
Model wybrany POMIAREM — inne kandydaty PRZEPISYWAŁY wypowiedź użytkownika, a bezpiecznik długości
tego nie łapie (parafraza ma WYŻSZE podobieństwo niż wierny wynik). AI Manager przyjął to
i **zapisał u siebie jako powód, nie jako dług**. Nie przepinaj bez zadania dobranego pod WIERNOŚĆ.
