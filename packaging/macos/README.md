# macOS packaging (Etap M4)

Tu trafi: skrypt budujący `.app`/`.dmg` (py2app lub PyInstaller),
`Info.plist` z uprawnieniem mikrofonu (NSMicrophoneUsageDescription),
krok podpisu/notaryzacji (sterowany przez ../signing.conf).
