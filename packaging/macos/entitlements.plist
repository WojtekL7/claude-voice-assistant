<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Uprawnienia używane przy podpisie z hardened runtime (gdy MACOS_SIGN=true w
  packaging/signing.conf). Build niepodpisany ich nie potrzebuje.

  - device.audio-input: mikrofon (dyktowanie / STT).
  - allow-jit, allow-unsigned-executable-memory: wymagane przez silnik QtWebEngine
    (Chromium) napędzający WebTerminal.
  - disable-library-validation: pozwala załadować dylib-y dołączone przez
    PyInstaller (niepodpisane tym samym Team ID).
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
