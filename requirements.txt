# Claude Voice Assistant - Dependencies
# GUI
PyQt5>=5.15.0
PyQt5-Qt5>=5.15.0
PyQt5-sip>=12.0.0
# WebTerminal (xterm.js w QtWebEngine) — terminal wieloplatformowy (M2).
# Na macOS/Windows jedyny backend terminala; na Linuksie opcjonalny.
PyQtWebEngine>=5.15.0

# Terminal Emulator (QTermWidget) — TYLKO Linux (x86_64)
# Install from wheels/qtermwidget-1.4.0-cp310-abi3-manylinux_2_17_x86_64.whl
# Or build from source: https://github.com/lxqt/qtermwidget (tag 1.4.0)
# Requires system package: libqtermwidget5-1-dev

# Terminal/PTY communication
# Unix (Linux/macOS): pexpect/ptyprocess. Windows: pywinpty (ConPTY) — instaluje
# się TYLKO na Windows dzięki markerowi środowiska (pip pomija go gdzie indziej).
pexpect>=4.8.0
ptyprocess>=0.7.0
pywinpty>=2.0.10; sys_platform == "win32"

# Text-to-Speech
edge-tts>=6.1.0
pygame>=2.5.0

# Speech-to-Text
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0          # używane przez edge-tts (TTS)
# UWAGA: NIE dodawać 'asyncio' jako pakietu pip — asyncio jest modułem
# standardowym Pythona 3.x; pakiet pip 'asyncio' to przestarzały backport,
# który psuje instalację na nowoczesnym Pythonie (m.in. na macOS).

# Text cleaning for TTS — OPCJONALNE (słownikowe czyszczenie wymowy).
# Kod działa bez tego (patrz ENCHANT_AVAILABLE w src/core/text_cleaner.py — przy
# braku po prostu pomija filtr słownikowy). Wymaga SYSTEMOWEJ biblioteki enchant,
# więc NIE instalujemy domyślnie, by `pip install -r requirements.txt` działał
# wszędzie (Linux/macOS/Windows). Aby włączyć:
#   Linux:  sudo apt-get install libenchant-2-dev aspell-pl aspell-en && pip install pyenchant
#   macOS:  brew install enchant && pip install pyenchant
# pyenchant>=3.2.0

# Packaging
pyinstaller>=6.0.0
