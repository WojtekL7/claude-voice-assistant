"""Synchronizacja agentów przez chmurę (Faza 1 — silnik paczki 'mózgu').

Publiczne:
- `CloudProvider` / `InMemoryProvider` (`cloud_provider`) — abstrakcja magazynu.
- `export_bundle` / `import_bundle` (`agent_bundle`) — paczka 'mózgu' BEZ sekretów.
"""
